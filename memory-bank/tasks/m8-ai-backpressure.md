# M8 — AI Backpressure (Вариант A)

- Версия: 2025-11-18

## Контекст
- Отчёт [REPORT-ai-throughput-20251116](../../reports/REPORT-ai-throughput-20251116.md) определяет вариант A как минимальную реализацию с локальным семафором и очередью в памяти воркера.
- Цель блока М8.Ш4 — удерживать управляемую параллельность AI-запросов, фиксировать параметры очереди и предоставить операционные инструкции до перехода к шагу 5.

## Конфигурационная цепочка
1. **KV (`AI_QUEUE_CONFIG`)** — источник истины для продового воркера. Значение хранится в JSON:
   ```json
   {
     "AI_MAX_CONCURRENCY": 4,
     "AI_QUEUE_MAX_SIZE": 64,
     "AI_TIMEOUT_MS": 12000,
     "AI_RETRY_MAX": 2,
     "AI_BASE_URLS": [
       "https://api.openai.com/v1/responses",
       "https://api.openai.com/v1/responses?cf_region=eu"
     ],
      "AI_ENDPOINT_FAILOVER_THRESHOLD": 2
   }
   ```
2. **ENV** — fallback при отсутствии ключа в KV (например, при локальной отладке или падении KV). Используем те же ключи окружения.
3. **Дефолты в коде** — активируются при отсутствии значений в KV и Env (4 / 64 / 12000 / 2). Все ветви фиксируются в логах `[ai][config]`.
4. **AI_BASE_URLS** — массив HTTPS-эндпоинтов Responses API, задаётся в `AI_QUEUE_CONFIG` или `AI_BASE_URLS` (ENV). Формат: JSON-массив строк, каждая должна указывать на `https://.../v1/responses` (допустимы query-параметры). При отсутствии переопределений активен дефолт `https://api.openai.com/v1/responses`.
5. **AI_ENDPOINT_FAILOVER_THRESHOLD** — количество подряд идущих retryable таймаутов/429/5xx перед переключением на резервный эндпоинт. Значение читается из `AI_QUEUE_CONFIG` → `AI_ENDPOINT_FAILOVER_THRESHOLD` (ENV) → дефолт `3`.

## Схема очереди и семафора
- Глобальный семафор удерживает максимум `AI_MAX_CONCURRENCY` одновременных fetch’ей OpenAI; ожидание выполняется через `await semaphore.acquire()`.
- Очередь размером `AI_QUEUE_MAX_SIZE` хранит промисы ожидания; переполнение → мгновенный отказ с дружелюбным сообщением (`getFriendlyOverloadMessage`).
- Таймаут на выполнение одного запроса — `AI_TIMEOUT_MS`, после чего запрос прерывается и пробует повтор до `AI_RETRY_MAX`.
- Счётчики `{ active, queued, droppedSinceBoot, lastDropAt }` и гистограмма ожидания публикуются в `/admin/diag?q=ai-queue`.

## Логирование
- `[ai][queue_enter]` / `[ai][queue_leave]` — фиксируют попадание и выход из очереди (payload: `requestId`, `userHash`, `queuedMs`, `active`).
- `[ai][retry]` — попытка повторного вызова (поля `attempt`, `status`, `reason`).
- `[ai][timeout]` — превышен `AI_TIMEOUT_MS`, указывает длительность и номер попытки.
- `[ai][dropped]` — очередь переполнена, включает `queueSize`, `maxQueue`, `requestId`.
- `[ai][request|ok|retry|timeout]` дополнены полями `endpointId` и `baseUrl`, чтобы tail-лог отражал активный эндпоинт. При достижении порога `AI_ENDPOINT_FAILOVER_THRESHOLD` логируется `[ai][endpoint_failover] { from, to, reason, threshold, consecutiveFailures }`.

## Failover эндпоинтов
- Конфигурация `AI_BASE_URLS` задаёт упорядоченный список base URL, первый считается активным. Счётчик `consecutiveRetryableErrors` ведётся отдельно для каждого эндпоинта.
- При каждом retryable `timeout/429/5xx` активный эндпоинт увеличивает счётчик. Когда значение достигает `AI_ENDPOINT_FAILOVER_THRESHOLD`, происходит переключение на следующий base URL (по кругу) с логом `[ai][endpoint_failover]`.
- Метаданные ответа (`ai.reply`) включают `endpointId` и `baseUrl`, что позволяет реконструировать источник текста без доступа к теле запроса.
- `/admin/diag?q=ai-queue` публикует блок `endpoints` с полями `{ activeBaseUrl, backupBaseUrls, failoverCounts }`, где `failoverCounts` — накопленные переключения по каждому `endpoint_N`.

## Дружелюбный отказ
- Сообщение: `"Перегружены запросами 😔 Дай нам пару секунд и попробуй ещё раз."`
- Отправляется только при переполнении очереди; webhook возвращает HTTP 200, чтобы не повторять запрос Telegram.

## Диагностики и тревоги
- `/admin/diag?q=ai-queue` → 200, тело:
  ```json
  {
    "status": "ok",
    "maxConcurrency": 4,
    "maxQueue": 64,
    "active": 0,
    "queued": 0,
    "droppedSinceBoot": 0,
    "lastDropAt": null,
    "endpoints": {
      "activeBaseUrl": "https://api.openai.com/v1/responses",
      "backupBaseUrls": [],
      "failoverCounts": { "endpoint_1": 0 }
    }
  }
  ```
- Порог тревоги **Warning** — `queued >= 48` (75% очереди) дольше 30 секунд.
- Порог тревоги **Critical** — `droppedSinceBoot > 0` или `active == maxConcurrency` более 60 секунд вместе с `queued >= 64`.

## Чек-лист готовности (Шаги 4.1–4.6)

### 4.1. Конфигурация и чтение KV→Env→Default
- **Результат:** воркер читает `AI_MAX_CONCURRENCY`, `AI_QUEUE_MAX_SIZE`, `AI_TIMEOUT_MS`, `AI_RETRY_MAX` в приоритете KV (`AI_QUEUE_CONFIG`) → Env → дефолты, источники фиксируются в логах `[ai][config]`.
- **Внутренние проверки:** ревью `readConfig`, ручной запуск `/admin/diag?q=ai-queue` с переключением KV → Env, сверка значений в логах.
- **Внешние зависимости:** актуальный KV-ключ `AI_QUEUE_CONFIG` на продовом воркере, доступ к управлению env-переменными.
- **Статус:** ✅ Завершено 16.11.2025, параметры 4 / 64 / 12000 / 2 активны.

### 4.2. Семафор и очередь
- **Результат:** глобальный семафор ограничивает конкуренцию до `AI_MAX_CONCURRENCY`, очередь отклоняет переполнения дружелюбным сообщением.
- **Внутренние проверки:** нагрузочный скрипт (12 параллельных запросов) подтверждает `active ≤ 4`, `queued ≤ 7`; логи `[ai][queue_enter|queue_leave|dropped]` присутствуют.
- **Внешние зависимости:** доступ к нагрузочному скрипту `load-tests/send-messages.mjs` во внутреннем репозитории, тестовый чат Telegram.
- **Статус:** ✅ Завершено 16.11.2025, переполнения логируются, отказ 200 с сообщением «Перегружены запросами 😔…».

### 4.3. Расширенное логирование и ретраи
- **Результат:** `AI_TIMEOUT_MS = 12000` с AbortController, `AI_RETRY_MAX = 2` с экспоненциальным backoff; логи `[ai][retry]`, `[ai][timeout]` фиксируют попытки и причины.
- **Внутренние проверки:** имитация 502 → успешный повтор, принудительный таймаут на 13 000 мс, проверка логов `[ai][retry|timeout]`.
- **Внешние зависимости:** тестовый мок OpenAI/Telegram для генерации 5xx и таймаутов.
- **Статус:** ✅ Завершено 16.11.2025, логи соответствуют конфигу.

### 4.4. Диагностика `/admin/diag?q=ai-queue`
- **Результат:** маршрут возвращает `status`, `maxConcurrency`, `maxQueue`, `active`, `queued`, `droppedSinceBoot`, `lastDropAt`; тревоги: Warning (`queued ≥ 48` >30 с) и Critical (`droppedSinceBoot > 0` или `active == maxConcurrency` 60 с + полный `queued`).
- **Внутренние проверки:** ручные вызовы до/после нагрузки, сравнение со счётчиками семафора.
- **Внешние зависимости:** публикация обновлённого маршрута на рабочем воркере, доступ к Cloudflare Tail для валидации.
- **Статус:** ✅ Завершено 16.11.2025, после smoke `active:0`, `queued:0`, `droppedSinceBoot:0`.

### 4.5. Smoke-прогон с нагрузкой
- **Результат:** сценарий и артефакты задокументированы в `../logs/ai-queue-smoke-2025-11-16.md` (tail-лог + снимок диагностики), служат контрольной точкой перед шагом 5.
- **Внутренние проверки:** повторный запуск скрипта нагрузки, подтверждение `droppedSinceBoot = 0`, `queued < 48` и TTFB ≤ 3.5 с.
- **Внешние зависимости:** артефакты `logs/ai-queue-smoke-2025-11-16.log` и `logs/ai-queue-smoke-2025-11-16-diag.json` во внешнем хранилище команды.
- **Статус:** ⏳ Требуется повтор до старта следующего шага.

### 4.6. Gate 4.x.a / 4.x.b
- **Результат:** подготовлены gate-чекпоинты для перехода к шагам 4.5–5: документация (`RoadMap.md`, `memory-bank/tasks/m8-ai-backpressure.md`) обновлена до версии 2025-11-18, `npm run check:roadmap` подтверждает синхронизацию.
- **Внутренние проверки:** запуск `npm run check:roadmap`, ревью документов и сверка с отчётом [REPORT-ai-throughput-20251116](../../reports/REPORT-ai-throughput-20251116.md).
- **Внешние зависимости:** актуализация smoke-артефактов (tail-лог, diag JSON) и подтверждение их размещения в общем хранилище для команды.
- **Статус:** ⏳ Требует обновления артефактов и фиксации gate-чека перед шагом 5.
