# `/start` локальный привет — 2025-11-21

- **Сценарий:** whitelisted администратор запустил `/start src_TEST-CAMPAIGN`, затем отправил дополнительные сообщения без payload.
- **Цель шага 5.2:** убедиться, что приветствие выполняется локально без вызова OpenAI, `utm_source` не перезаписывается, fallback «⚠️ → 🔁💬» отсутствует.
- **Метрики:**
  - `openAiRequestsDuringStart:0`, `max_retries_exceeded:0`.
  - `ai_queue_active:0`, `ai_queue_queued:0`, `ai_queue_dropped:0` на всём интервале (очередь не задействована).
  - `utm_rows_before=1`, `utm_rows_after=1`, `utm_sources=["src_TEST-CAMPAIGN"]` (сохраняется первая запись).
  - `selftest.openAiOk/selftest.telegramOk` не выполнялись в этом прогоне (`n/a`).
- **Логи:** tail `logs/start-greeting-tail.json` (во внешнем хранилище) фиксирует последовательность `utm-tracking` → `sendTyping` → `sendText 200` без `[ai]` блоков.
- **Ссылки:** памятка [`memory-bank/operations/start-command.md`](../operations/start-command.md) описывает сценарий и чек-лист повторных проверок.
