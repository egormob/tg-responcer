# Features Layer

Дополнительные функции (рассылки, экспорт, лимиты и др.) подключаются к ядру через этот каталог.

## Admin access

Список администраторов бота хранится в KV-биндинге `ADMIN_TG_IDS` в формате JSON:

```json
{ "whitelist": ["123", "456"] }
```
