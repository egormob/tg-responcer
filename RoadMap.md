# RoadMap

## Gate-чек переключения майлстоунов
- Перед стартом нового майлстоуна выполняем обязательный gate-чек и подтверждаем, что предыдущий не содержит открытых статусов `⏳` или `🚧`.
- Запускаем `npm run check:roadmap`, фиксируем результат в журнале и не продолжаем, если скрипт сигнализирует о нарушении линейности майлстоунов.
- Дополнительно вручную просматриваем шаги предыдущего майлстоуна: если есть блокеры, открываем «Задачу-торможение» и остаёмся на текущем майлстоуне до полного закрытия.

## Блок проверок после фиксов Telegram ID
1. **Lossless `chat_id`.** Проверяем, что webhook и вспомогательные хелперы сохраняют `chat_id` без преобразований (`chat_id_raw == chat_id_used`) для всех веток (`user`, `admin`, `broadcast`).
2. **Диагностика вебхука.** `/admin/selftest` и `/admin/diag` обязаны логировать `chat_id_raw/chat_id_used`, выбранный маршрут и HTTP-код Telegram для последних отправок.
3. **Синхронизация `knownUsers`.** Очистка через `/admin/known-users/clear` должна влиять на тот же сегмент, что использует webhook; после фикса выполняем smoke-тест «self-test → ручной запрос» и убеждаемся в повторной регистрации ID.
4. **Контроль рассылки.** Проверяем, что механика рассылок отклоняет усечённые идентификаторы и логирует нарушения; перед активацией рассылок выполняем smoke-тест отправки по whitelist.
5. **Последующие ретесты.** После каждого фикса запускаем регрессионный self-test и диплинк обычного пользователя; результаты заносим в `memory-bank/external-checks/` и подтверждаем восстановление диалога.

## Контекст и цели
- Репозиторий `tg-responcer` управляется по базовому протоколу из `Protocol.md`.

- Дорожная карта служит единственным источником истины о шагах, проверках и состоянии создания Telegram-бота на OpenAI Responses через Cloudflare Workers.
- **Цель проекта:** собрать устойчивого Telegram-бота с модульной архитектурой: базовый диалоговый контур (ядро + память), выгрузка диалогов, ручные рассылки, лимиты на ответы и индикация `typing…`. Промпты и инструменты настраиваются в OpenAI Responses, код работает через `/v1/responses`, указывая `model` и при необходимости опубликованный `prompt.id` (`pmpt_…`).

## Релизы и текущий фокус

### Навигационный блок
- **М8. Broadcast Feature**
  - [x] Gate-чек М8 закрыт: работа разблокирована.
  - [x] М8.Ш0 Очередь к командам `/broadcast` с мгновенной отправкой.
  - [ ] М8.Ш2 Ограничения памяти и ограниченные обновления.
  - [x] М8.Ш3 Лабелинг/шумка текста в объявлениях `/send` восстановлены на коммите f0fb1fa.
  - [x] Глобальный лимитер 28 rps + джиттер зафиксирован и включён (см. [памятку](memory-bank/operations/broadcast-operations.md#глобальный-лимитер-рассылки)).
  - [x] М8.Ш4 Форма для выбора подтверждения и лимит текста: подтверждён поток «новый текст → подтверждение → отправка», лимит 3970 символов срабатывает (см. [diag](memory-bank/diagnostics.md#diag-20251201)).
  - [ ] Guardrail: чекпоинты и авто-возобновление рассылки (наблюдаемость М8) — `/admin/diag?q=broadcast` фиксирует `jobId`/курсор, а `/broadcast_resume` возобновляет прерванный поток без пересборки аудитории.
    - [x] **М8.J1 — jobId-диспетчер, pause/resume и KV-чекпоинты.** Единая активная рассылка, батчинговые чекпоинты в `broadcast:progress:<jobId>` (смещение, delivered/failed, throttled429, хеши текста/аудитории, параметры лимитера), аварийные стоп-сигналы (OOM/длинный `retry_after`/ручная отмена) с уведомлением админа и кнопками «Продолжить/Отменить». Финал: очистка чекпоинтов, запись итога в телеметрию и обновление RoadMap/памяток.
    - [x] **М8.J2 — Диагностика и UX pause/resume.** `/admin/diag?q=broadcast` и админское уведомление показывают активный/paused `jobId`, remaining/total, причину остановки, TTL чекпоинта и команды «Продолжить/Отменить». Памятки фиксируют сценарий pause/resume и правила одного активного job. Финал: обновить RoadMap и `memory-bank/operations/broadcast-operations.md` (PR #332, diag/уведомления синхронизированы, интеграционные тесты ждут асинхронную доставку).
    - [ ] **М8.J3 — Нагрузочная устойчивость (100k+ адресатов).** Конфиг темпа/памяти (лимит батча по числу получателей и суммарному тексту), авто-деградация `concurrency/maxRps` при 429/росте памяти, watchdog с логированием, регрессионные тесты pause/resume на очередях ≥100k без дублей. Финал: записать результаты и выводы в RoadMap и памятки.
  - [ ] Фиксация отписки для рассылок: при событиях `my_chat_member` со статусами вроде `kicked` или при ошибке Telegram `403 Forbidden: bot was blocked by the user` проставлять флаг `blocked_at`/`unsubscribed` и исключать адресата из рассылок.
  - [ ] Дорожная карта UX рассылок (поля и напоминания).
  - Текущая диагностика М8:
    - [x] (Шаг 0) Подтверждены безопасные диапазоны отправки (Self-test) — две диагностики `/admin/diag?q=broadcast` вернули `status: ok`, `throttled429: 0` (см. [diag](memory-bank/diagnostics.md#diag-20251201)).
    - [x] Подготовить безопасные тестовые рассылки (буфер наполняется) — тестовая рассылка по D1-реестру на 5 адресатов завершилась без сбоев воркера (см. [diag](memory-bank/diagnostics.md#diag-20251201)).
    - [x] Смоук-тест отправки для whitelisted-пользователя (команда работает, данные пишутся) — сценарий `/broadcast` выполнен админом `136236606`, состояния pending/confirmation в логах (см. [diag](memory-bank/diagnostics.md#diag-20251201)).
    - [x] Расширить проверку лимитов на текст и тип сообщений (в боевом режиме, диаг) — отклонён текст 4085 символов с подсказкой на лимит 3970, подтверждён переход к повторному вводу (см. [diag](memory-bank/diagnostics.md#diag-20251201)).
    - [ ] Восстановить первый прогон рассылки после нагрузки; при необходимости отделить правила для ML.

- **Промежуточный майлстоун «Фиксы и редизайн» (привязать к М9)**
  - [x] Переиздать UTM-метрику с async записью (боевые тесты на регистрациях подтверждены).
  - [ ] Проверка и фиксы UX: форма подтверждений и лимит текста — правки в макетах, интеграция.
  - [ ] Уточнить AI Queue retries: нагрузка, логирование, алерты (`ai_queue.retry_after`, `ai_queue.retry_limit`, `ai_queue.error_code`).
  - [ ] Перенести stress-test loop в отдельный тред (`ai_queue/test-runner`).
  - [ ] Выкатить схемы (D1) для ML-выгрузки: utm/export/time.
  - [ ] Политика удаления: лимиты `/bcast` и экспорт — форматы сообщений (`ai-export-template`).

- **М10. Post-mortem и модели (Comms & models evaluation)**
  - [ ] Сбор метрик по всем сценариям (рассылки, сервисные команды, UI).
  - [ ] Ревизия моделей (PMPT) для текстов рассылок.
  - [ ] Провести ретро, обновить README и RoadMap.

После каждого шага обновлять этот список и соответствующие памятки. Проверки («ПРОВЕРКА x.y») — обязательные стоп-пойнты перед переходом к следующему шагу.

## Полная дорожная карта (A–E)
Отражает перенесённые элементы и их связь с текущей нумерацией (М8–М10), чтобы задачи не терялись при последующих обновлениях.

### Майлстоун A — Диагностика и стресс-тесты (привязка к М9)
- [x] Encode system conversation turns as `input_text` в OpenAI-адаптере — выполнено и учтено в тестах; контрольный статус закреплён за блоком наблюдаемости М9. Артефакты: [memory-bank/tasks/roadmap-a-e-artifacts.md#a1-encode-input_text](memory-bank/tasks/roadmap-a-e-artifacts.md#a1-encode-input_text).
- [x] AI Queue stress-тесты с `STRESS_TEST_ENABLED` / `STRESS_TEST_MAX_RETRIES_EXCEEDED` и политикой ретраев Variant C — выполнено, результаты и кодпоинты диаг/ретраев синхронизированы со шагами М9.Ш4–М9.Ш5. Артефакты: [memory-bank/tasks/roadmap-a-e-artifacts.md#a2-ai-queue-stress](memory-bank/tasks/roadmap-a-e-artifacts.md#a2-ai-queue-stress).

### Майлстоун B — UTM и экспорт (привязка к М8)
- [x] Гарантированная async UTM-регистрация и UTM-алерты — боевой тест подтверждён, запись UTM при регистрации пользователя работает; статус закреплён за М8 как основа для последующих шагов. Артефакты: [memory-bank/tasks/roadmap-a-e-artifacts.md#b1-utm-async](memory-bank/tasks/roadmap-a-e-artifacts.md#b1-utm-async).
- [x] Экспорт CSV с пагинацией и UTM — боевой тест подтверждён, экспорт работает; контрольный статус связан с М8 и тестами экспорта. Артефакты: [memory-bank/tasks/roadmap-a-e-artifacts.md#b2-export](memory-bank/tasks/roadmap-a-e-artifacts.md#b2-export).

### Майлстоун C — UX-фиксы рассылки (привязка к М8)
- [x] Незавершённые UX-фиксы `/broadcast` закрыты (М8.Ш4): устранено двойное предупреждение о лимите текста, отправка подтверждена в рабочем сценарии. Артефакты: [memory-bank/tasks/roadmap-a-e-artifacts.md#c1-ux](memory-bank/tasks/roadmap-a-e-artifacts.md#c1-ux).

### Майлстоун D — Observability & On-call (привязка к М9)
- [x] Observability & On-call проверки (soft self-test 200, разделение retriable/non-retriable, разграничение user/admin) проведены и закреплены за М9; использование самотеста и тревог отражено в памятках. Артефакты: [memory-bank/tasks/roadmap-a-e-artifacts.md#d1-observability](memory-bank/tasks/roadmap-a-e-artifacts.md#d1-observability).

### Майлстоун E — Comms & models evaluation (привязка к М10)
- [ ] Блок коммуникаций и оценки моделей (Comms & models evaluation) остаётся в backlog М10; требуется дополнить критериями и тестами перед стартом майлстоуна. Артефакты: [memory-bank/tasks/roadmap-a-e-artifacts.md#e1-comms](memory-bank/tasks/roadmap-a-e-artifacts.md#e1-comms).

- **Релиз 1. Диалоговый контур.** Пользовательский диалог с Responses (5 экземпляров GPT в промпте), поддержка системного промпта, файлов и инструментов Responses, хранение памяти диалога в D1 и восстановление контекста при каждом запросе.
  - Функциональный охват: майлстоуны 3–5 + проверка `/admin/envz`, активация Responses и валидация KV/лимитов.
  - Блокеры: повторяющийся сброс переменных `OPENAI_MODEL`/`OPENAI_PROMPT_VARIABLES` и биндинга `RATE_LIMIT_KV` при деплое из Cloudflare Dashboard (см. `memory-bank/issues/cloudflare-env-reset.md`). Для релиза необходимо автоматизировать применение конфигурации и исключить ручные деплои, иначе воркер теряет настройки после каждой публикации.
  - [x] Восстановить диалоговый контур — lossless Telegram ID parser ([memory-bank/issues/2025-11-16-telegram-id-lossless-parser.md](memory-bank/issues/2025-11-16-telegram-id-lossless-parser.md)). Опираться на памятку с фактом диагностики (DIAG-PACKET) и закрыть зависимости: lossless-парсер, guard'ы в `http/telegram-webhook.ts`, расширенная диагностика `/admin/selftest` и `/admin/diag` для Telegram. Lossless-парсер подтверждён: `chatIdRawType`/`chatIdNormalizedHash` стабилизированы, диагностический self-test задокументирован.
- **Релиз 2. Экспорт истории.** Расширение `/admin/export` до штатного процесса выгрузки диалогов в Excel (на базе CSV). Зависит от устойчивого хранения диалогов из релиза 1 и проверки стабильности экспорта на продовом воркере.
  - Самовосстановление `utm_source` в D1: адаптер после пяти fallback-записей вызывает `PRAGMA table_info(users)` и при наличии колонки возвращается к основной схеме, фиксируя пары логов `[d1-storage] utm_source column missing, disabling usage` → `[d1-storage] utm_source column restored, re-enabling usage`.
- **Релиз 3. Рассылки.** Управляемая отправка сообщений пользователям (фича Broadcast) с учётом лимитов Telegram. Требует завершения работ по KV-лимитам и стабилизации мониторинга.
  - Перед запуском каждой рассылки администратор сверяется с памяткой минимальной отправки (`memory-bank/operations/broadcast-operations.md#памятка-минимальной-рассылки`) и подтверждает соответствие лимитам Telegram.

**Текущее состояние:** майлстоун М7 закрыт: 2025-11-08 внешняя проверка `/admin/export` на продовом воркере прошла успешно после восстановления `ADMIN_EXPORT_TOKEN` и биндинга `ADMIN_EXPORT_LOG`, а 2025-11-09 подтверждена работоспособность чат-команды `/export` с общим whitelist-адаптером. Активная работа смещена на шаг М8.Ш3 (внешние действия для рассылок) и дальнейшие майлстоуны наблюдаемости.

## Принципы работы
1. Следовать протоколу: поддерживать артефакты `RoadMap.md` и `memory-bank/`, хранить логи/снапшоты вне git. Команда «Следуй дорожной карте» ведёт по шагам RoadMap и при переходе к проверкам автоматически переключает исполнителя на раздел «Протокол проверки».
2. Работать партиями до ≈150 файлов, контролировать `git diff --stat`, выполнять проверки после каждой партии.
3. Каждый подпункт дорожной карты завершать отдельным коммитом и фиксировать прогресс здесь.
4. Поддерживать линейность истории ветки `work`, избегать расходящихся коммитов.
5. Перед началом задачи сверяться с архитектурой, рисками и актуальной картиной системы.
6. Перед подготовкой PR синхронизировать ветку `work` с целевой базой, убедиться в отсутствии merge-конфликтов и зафиксировать
   в журнале, если конфликт решался вручную.

## Протокол проверки

0. **Подготовка**  
  - Убедиться в актуальности рабочей ветки, зависимостей и конфигураций окружения; подтвердить доступность внешних сервисов и тестовых стендов.
  - Для деплоев Cloudflare дополнительно проверить, что в Dashboard сохранены значения `OPENAI_MODEL`, `OPENAI_PROMPT_ID`, `OPENAI_PROMPT_VARIABLES`, а биндинги `DB` и `RATE_LIMIT_KV` указывают на существующие ресурсы (см. `memory-bank/issues/cloudflare-env-reset.md`).
  - При задачах, затрагивающих KV, свериться с чек-листом «KV Namespace ID — обязательная фиксация» (`memory-bank/infrastructure.md`) и подтвердить, что в `wrangler.toml` записаны актуальные Namespace ID для `RATE_LIMIT_KV`, `ADMIN_TG_IDS`, `ADMIN_EXPORT_LOG`.
   - Обновить и проверить тестовые данные/фикстуры, зафиксировать исходные метрики либо снимки состояния системы.
   - При незавершённой подготовке остановить процесс и устранить блокирующие факторы перед переходом к дальнейшим шагам.

1. **Планирование проверок**  
   - Составить последовательность внутренних (код) и внешних проверок с учётом зависимостей, рисков и предполагаемых выходных артефактов.  
   - Отметить проверки, которые можно выполнять параллельно, назначить ответственных, зафиксировать режим выполнения (код/план) и точки синхронизации общего прогресса.  
   - Для параллельных потоков определить механизмы обмена артефактами и порядок объединения результатов без конфликтов.

2. **Проведение проверок на уровне кода**  
   - Для каждой проверки подробно описать цель, ожидаемые результаты, входные данные и критерии завершения.  
   - Подготовить матрицу альтернативных методик с приоритетами, требуемыми ресурсами и ожидаемыми результатами ещё до запуска основной проверки.  
   - Заранее определить критерии, сколько альтернатив разрешено для критичных и некритичных проверок и в каких случаях требуется немедленно запрашивать внешнюю помощь.  
   - Фиксировать принятые решения и фактические результаты; при исчерпании матрицы или отсутствии необходимых условий эскалировать задачу во внешнюю проверку.

3. **Журналирование результатов**  
   - Записывать в журнал все проверки — успешные, неуспешные и заблокированные — с датой, исполнителем, командой/сценарием, параметрами окружения и ссылками на логи.  
   - Прикладывать артефакты (логи, снимки, diff) к каждой записи и указывать формальный статус (успех/неуспех/заблокировано).  
   - Обновлять дорожную карту на основании статусов, чтобы отразить фактический прогресс и выявленные блокеры.

4. **Реагирование на дефекты и восстановление работоспособности**  
   - На каждый обнаруженный дефект формировать задачу-исправление с указанием критичности, ответственного, сроков и зависимости от других работ.  
   - Составлять план ретеста и, при необходимости, регрессионных проверок; выполнять их до перехода к следующим блокам и фиксировать результаты.  
   - Обновлять журнал и дорожную карту после ретеста; в случае повторного провала автоматически возвращать задачу в активную работу до полного устранения проблемы.

## Анализ рисков и допущений

- **Инфраструктура.** Предполагается доступ к Cloudflare Workers, KV, D1, Queues, Telegram Bot API и OpenAI Responses. Любые изменения тарифов или API требуют синхронизации с владельцем продукта до начала работ.
- **Безопасность секретов.** Все секреты заносятся через `wrangler secret`, не хранятся в репозитории и обновляются по чек-листу из `memory-bank/`.
- **Релизы.** Единственный способ деплоя — `wrangler deploy` из проверенной ветки `work` с тегированием релизов. После успешного прохождения внешних проверок майлстоуна фиксируем стабильный прод аннотированным тегом `prod-YYYY-MM-DD[-label]`, ссылку на него сохраняем в `memory-bank/stable-builds.md`. Hotfix вне дорожной карты фиксируется в журнале прогресса отдельной записью.
- **Окна недоступности.** Любые миграции D1 или крупные рассылки согласуются заранее; при необходимости паузы в приёме вебхуков включается maintenance-флаг в KV и уведомляется команда.
- **Внешние зависимости.** Telegram и OpenAI могут возвращать недокументированные ошибки — каждая точка вызова должна иметь защиту таймаутом, повтором и fallback-сообщением.

Пока допущения выполняются, дорожная карта остаётся валидной. При нарушении допущения шаг пересматривается до начала реализации.

## Структура

**Цель:** устойчивый диалоговый контур с возможностью подключать апгрейды (экспорт, рассылка, лимиты) без изменений и рисков для базы.

**Слои и их роли:**
- **Ядро (`core/`)** — последовательность шагов диалога (лимит → запись → ответ ИИ → запись → отправка). Никаких SDK/HTTP. Статус: **FROZEN**.
- **Порты (`ports/`)** — четыре стабильных интерфейса: `MessagingPort` (`sendTyping`, `sendText`), `AiPort` (`reply({ userId, text, context }) → { text }`), `StoragePort` (`saveUser`, `appendMessage`, `getRecentMessages`), `RateLimitPort` (`checkAndInc → "ok" | "limit"`).
- **Адаптеры (`adapters/*`)** — реализации портов (Telegram, OpenAI Responses, D1, KV и т.п.).
- **NOOP-адаптеры (`adapters-noop/*`)** — безопасные заглушки для каждого порта по умолчанию.
- **Композиция (`composition/`)** — сборка адаптеров, регистрация фич и чтение KV-флагов.
- **HTTP (`http/`)** — только роутинг: `POST /webhook/:secret` (включая обработку команд/админа до ядра), `GET /healthz`, `/admin/*` (роуты фич).
- **Фичи (`features/*`)** — апгрейды как отдельные модули (экспорт CSV, рассылка, лимиты). Подключаются через `composition/register-features.ts` в `try/catch`; при сбое ядро остаётся рабочим.

**Инварианты Responses:**
- Воркёр вызывает только `POST https://api.openai.com/v1/responses` с явным `model` из `env.OPENAI_MODEL`; при необходимости использует опубликованный `prompt.id` (`pmpt_…`) и переменные.
- Ассистентские идентификаторы (`asst_…`) не используются; состояние продолжений храним через `previous_response_id`.
- При ответе показываем `typing…`, затем отправляем финальное сообщение.
- Контекст для модели — короткий хвост из `StoragePort.getRecentMessages`, передаваемый через поле `input`.

**Хранилища и флаги:**
- Диалоги и пользователи — Cloudflare D1 (Paid до 10 ГБ, Free до 500 МБ).
- Лимиты и фич-флаги — Cloudflare KV (`STREAMING`, `BROADCAST`, `EXPORT_ENABLED`, `LIMITS_ENABLED` и др.).
- Админ-доступ: `/admin/*` через заголовок `X-Admin-Token`, чат-команды — whitelist `ADMIN_IDS`.

**Технические предохранители:** `CODEOWNERS` на `/core` и `/ports`, CI-гард, eslint-правила против сквозных импортов, ручной чек-лист релиза в `memory-bank/operations.md`.

## Целевая архитектура после перехода на Responses-only

- **AI-контур.** Адаптер `adapters/openai-responses` формирует запросы в `/v1/responses`, пробрасывая `model`, `input` и, при наличии, `prompt.id` с переменными. Для продолжений используется `previous_response_id`, а ответы читаются из `output_text` и item-потока.
- **Конфигурация окружения.** Воркёр ожидает `OPENAI_API_KEY`, `OPENAI_MODEL`, опционально `OPENAI_PROMPT_ID` (`pmpt_…`) и `OPENAI_PROMPT_VARIABLES` (JSON). Переменные валидируются и логируются без секретов.
- **Совместимость модулей.** Все активные адаптеры и фичи должны опираться на `PromptConfig`/`ResponseConfig` вместо `AssistantConfig`; legacy-поля `assistantId` вычищаются или закрываются фич-флагами до миграции.
- **Контроль рисков.** Перед подключением новых модулей выполняется проверка влияния на ядро и текущую рабочую связку Telegram ↔ ядро ↔ Responses.

## Правила
1. **FROZEN CORE.** `core/` меняем только с bump `core/VERSION` и ревью владельца.
2. **Стабильные порты.** Сигнатуры в `ports/` неизменны без версионирования и ревью.
3. **Только через порты.** Ядро не импортирует SDK, HTTP или биндинги Cloudflare.
4. **Команды до ядра.** `/admin`, `/broadcast` и служебные команды обрабатываются в `http/`, а не в `DialogEngine`.
5. **Фичи не трогают ядро.** Новые функции живут в `features/*` и подключаются через `composition/` под KV-флаги.
6. **NOOP по умолчанию.** При отсутствии адаптера/фичи используется заглушка, диалог остаётся работоспособным.
7. **Промпты вне кода.** Настройки Responses живут в опубликованных промптах; код читает `env.OPENAI_MODEL` и опционально `env.OPENAI_PROMPT_ID`/`PROMPT_VARIABLES`.
8. **Админ-охранник.** `/admin/*` требует `X-Admin-Token`; чат-команды доступны только `ADMIN_IDS`.
9. **Мягкая деградация.** Внешние вызовы защищены `try/catch` и таймаутами, ошибки логируем без остановки ядра.
10. **Троттлинг и лимиты.**
    - Применяется к массовым отправкам (рассылки, циклы). Обычные ответы ядра не троттлим.
    - Лимиты Telegram: безопасная скорость ≤28 сообщений/сек глобально (жёсткий предел ≈30/сек), ≤1 сообщение/сек в личный чат, ≤20 сообщений/мин в группы/каналы.
    - При `429 Too Many Requests` читаем `retry_after`, ждём указанное время, используем экспоненциальную задержку с джиттером, не более трёх ретраев, затем логируем и пропускаем адресата.
    - Отправляем батчами с паузами, порядок внутри батча не критичен.
    - При росте объёмов выносим рассылки в Cloudflare Queues (ретраи, батч-обработка) без изменений ядра.
11. **Тесты-сторожа.** Контракты портов, смоук `/webhook` с моками, проверка отключённых фич.
12. **Guardrails в репо.** `CODEOWNERS`, CI-гард и eslint обеспечивают замороженное ядро и корректные импорты.
13. **Retain & Rotate.** Политика хранения диалогов, крупные выгрузки — CSV с пагинацией или очередями.
14. **Флаги — источник правды.** Подключение/отключение фич происходит через KV и `composition/`.
15. **Журнал диагностик → RoadMap.** Каждый новый диагностический отчёт сначала заносится в таблицу `memory-bank/diagnostics.md#журнал-диагностик` (метрики `max_retries_exceeded`, `utm_rows`, `selftest.openAiOk`, `ai_queue.*`), затем на него ссылается соответствующий шаг RoadMap.

## Майлстоуны проекта

### Майлстоун 0. Операционная система управления проектом *(завершён 2025-10-23)*
**Цель:** зафиксировать протокол, дорожную карту и структуру памяти.

**Шаги и проверки:**
1. Создать `RoadMap.md`, каталог `memory-bank/`, зафиксировать журнал.
   - **Результат:** артефакты операционной системы присутствуют.
   - **Проверка:** `ls` показывает файлы; дорожная карта описывает цели и принципы.
2. Настроить контроль линейности ветки и журнал прогресса.
   - **Результат:** описаны меры контроля истории ветки и рисков.
   - **Проверка:** раздел «Принципы работы» отражает правила линейности.

**Риски и предохранители:** убедиться, что нет других файлов с конфликтующими инструкциями; зафиксировать ссылку на протокол в `memory-bank/`.

**Критерий приёмки:** RoadMap и память созданы, правила зафиксированы.

### Майлстоун 1. Responses-only Alignment & Baseline Verification
**Цель:** перестроить план работ под архитектуру `Responses + PromptID`, собрать проверки для действующих модулей и зафиксировать риски будущих подключений.

**Зависимости:** майлстоун 0.

**Шаги и проверки:**
1. Каталог проверок активной связки (ядро ↔ Telegram ↔ Responses).
   - **Результат:** в RoadMap и `memory-bank` зафиксирован перечень обязательных проверок/исправлений для модулей, уже задействованных в продовой связке. Список включает:
     - `core/DialogEngine` и `ports/*` — валидация отсутствия прямых зависимостей от `assistantId`, проверка последовательности вызовов.
     - `adapters/telegram` и `http/telegram-webhook` — smoke-тесты маршрутов, актуальность команд до ядра, контроль `typing`.
     - `adapters/openai-responses` — миграция на `PromptConfig`, проверка `env.OPENAI_MODEL/OPENAI_PROMPT_ID`, логирование `response_id`.
     - `composition/*` и `features/typing` — корректная сборка зависимостей, проверка KV-флагов.
     - Конфигурация окружения — наличие секретов, формат `pmpt_…`, trim значений.
   - **Проверка:** ревью RoadMap подтверждает наличие списка; в отчётах по шагам фиксируем прогресс и количество оставшихся проверок до закрытия майлстоуна.
   - **Статус:** ✅ Проведена сверка активных модулей: ядро и порты используют только `model`/контекст без `assistantId`, HTTP-роутер обрабатывает команды до ядра, Telegram-адаптер подтверждён тестами `messaging.test.ts`, а `adapters/openai-responses` логирует `response_id` и `prompt`. Проверки сведены в `memory-bank/active-stack-checks.md`, прогон `npm test` подтверждает целостность цепочки.
2. Проверка изоляции модулей, не участвующих в текущем контуре.
   - **Результат:** проведён аудит зависимостей между активными модулями и заготовками. Проверяются:
     - `features/limits`, `features/export`, `features/broadcast` на отсутствие импортов `adapters/openai-responses` и воздействия на текущую связку.
     - `adapters-noop/*`, `composition/register-features.ts` на корректные фич-флаги.
     - Тестовые файлы на отсутствие жёстких ссылок `assistantId`.
   - При наличии влияния добавляются временные флаги/заглушки или заводится «Задача-торможение».
   - **Проверка:** в RoadMap отмечены результаты аудита; при обнаружении связей открываются подзадачи с указанием файлов и требуемых фиксов.
   - **Статус:** ✅ Аудит изоляции выполнен: `features/limits` и `features/export` используют только порты и утилиты без прямых зависимостей от `adapters/openai-responses`, `adapters-noop` и `composition/compose.ts` не подтягивают Responses-функциональность по умолчанию, тестовые файлы не содержат ссылок на `assistantId`. Итоги сведены в `memory-bank/module-isolation-audit.md`, блокеров не обнаружено, `features/broadcast` ещё не реализована и остаётся под наблюдением на будущих шагах.
3. План ревизии модулей для будущих подключений.
   - **Результат:** составлен список модулей, требующих адаптации под `PromptID`, включая:
     - `adapters/d1-storage`, `features/export` — проверка формата сообщений и хранения `response_id`.
     - `features/limits`, `adapters/kv-rate-limit` — исключение ссылок на `assistantId`, согласование уведомлений с Responses.
     - `features/broadcast`, планируемые очереди — использование Responses без ассистента.
     - `features/observability`, `admin`-диагностика — обновление метрик/роутов под новую конфигурацию env.
   - Для каждого элемента описаны риски и проверки, которые нужно выполнить перед активацией.
   - **Проверка:** запись добавлена в журнал прогресса и памятки (`memory-bank/infrastructure.md`, `memory-bank/stable-builds.md`); наличие незакрытых проверок помечается как блокер для команды «Следуй дорожной карте».
   - **Статус:** ✅ План составлен: перечислены `adapters/d1-storage`, `features/export`, `features/limits`/`adapters/kv-rate-limit`, `features/broadcast`, `features/observability` и документация. Для каждого модуля описаны риски (потеря `response_id`, отсутствие троттлинга, устаревшие метрики) и проверки перед активацией. Детали перенесены в `memory-bank/infrastructure.md` и `memory-bank/stable-builds.md`, незакрытые проверки помечены как блокеры до апдейтов.
4. Обновление быстрой инструкции и мемори-банка под Responses-only.
   - **Результат:** `RoadMap.md` и памятки отражают новые правила: отказ от `assistantId`, требования к `OPENAI_MODEL`/`OPENAI_PROMPT_ID`, механизм «Задачи-торможения» при пропущенных критичных проверках.
   - **Проверка:** ревью документов подтверждает наличие инструкции; быстрый протокол содержит пункт о создании задачи-торможения и отчётности по проверкам.
   - **Статус:** ✅ RoadMap и памятки обновлены: быстрый протокол дополнен проверкой требований Responses-only, `memory-bank/README.md` фиксирует отказ от `assistantId` и правила валидации `OPENAI_MODEL`/`OPENAI_PROMPT_ID`.

**Риски и предохранители:** контролировать размер диффов (<200 строк), согласовывать изменения плана с владельцем продукта, не переходить к коду до утверждения списка проверок.

**Критерии приёмки:** RoadMap и память отражают новую архитектуру, список проверок утверждён, блокеры для будущих модулей задокументированы.

### Майлстоун 2. Frozen Core Scaffold
**Цель:** подготовить каркас воркера с замороженным ядром и портами, чтобы дальнейшие апгрейды подключались без риска.

**Зависимости:** майлстоуны 0–1.

**Шаги и проверки:**
1. Стартовая структура воркера.
   - **Результат:** каталоги `apps/worker-main/{core,ports,adapters,adapters-noop,composition,http,features,shared}` с базовыми индексами и заглушечными `index.ts`/`README` для ориентирования.
   - **Проверка:** `tree apps/worker-main -L 2` показывает структуру (в CI допускается `find apps/worker-main -maxdepth 2 -type d`).
   - **Внешние действия:** не требуются.
   - **Статус:** ✅ Каркас каталогов и заглушки созданы, структура соответствует плану.
2. Реализация `core/DialogEngine.ts` и контрактов.
   - **Результат:** минимальный контур диалога вызывает порты в последовательности «лимит → запись входа → ответ ИИ → запись ответа → отправка», интерфейсы экспортируются из `ports/`, файл помечен баннером FROZEN.
   - **Проверка:** юнит-тест `DialogEngine` с моками (`npm run test:core`) покрывает порядок вызовов и обработку ошибок; отчёт о покрытии сохраняется в `logs/`.
   - **Статус:** ✅ Контур реализован, тесты подготовлены (`vitest`). Запуск ожидает установки npm-зависимостей (в среде агента `npm install` завершился ошибкой 403).
3. Определение портов и NOOP-адаптеров.
   - **Результат:** интерфейсы для Messaging/Ai/Storage/RateLimit и заглушки, возвращающие безопасные значения и логирующие предупреждения, чтобы облегчить отладку; JSDoc поясняет контракт по таймингам и повторам.
   - **Проверка:** `npm run typecheck` проходит; unit-тесты NOOP подтверждают отсутствие побочных эффектов; eslint-правило запрещает прямые импорты в `core/`.
   - **Статус:** ✅ Контракты уточнены, заглушки реализованы, добавлены юнит-тесты (запуск тестов ожидает доступа к npm registry, `npm install` возвращает 403).
4. Композиция и HTTP-роутер.
   - **Результат:** `composition/compose.ts` выбирает адаптеры или NOOP в зависимости от доступных биндингов, `http/router.ts` объявляет `/webhook/:secret` и `/healthz`, webhook проверяет секрет и проксирует в `DialogEngine`.
   - **Проверка:** интеграционный тест с моками (`npm run test -- http-router`), ручной вызов `wrangler dev` + `curl /healthz` (сообщить результат); лог или скриншот команды сохраняется в `logs/`.
   - **Статус:** ✅ Реализованы композиция и HTTP-роутер с покрытием unit-тестами (`apps/worker-main/http/__tests__/router.test.ts`, `apps/worker-main/composition/__tests__/compose.test.ts`); роут `/healthz` возвращает `status: ok`, webhook валидирует секрет и проксирует вызов в `DialogEngine`.
5. Guardrails.
   - **Результат:** `CODEOWNERS`, CI-гард (GitHub Actions), eslint-правила запретов импортов, файл `core/VERSION` и скрипт `npm run validate-guards`, который срабатывает при изменении `core/` без bump версии.
   - **Проверка:** `npm run lint` без ошибок; `npm run validate-guards` отрабатывает без сбоев; визуальная проверка `CODEOWNERS`; вручную симулируем изменение в `core/` без bump и убеждаемся, что guard падает.
   - **Статус:** ✅ Добавлены `CODEOWNERS`, workflow CI (lint, test, validate-guards), правило `no-restricted-imports` и файл `core/VERSION`; guard скрипт блокирует изменения ядра без bump (локальный запуск требует `npm install`, которое в среде агента даёт 403 на npm registry).

**Риски и предохранители:** закрепить владельцев `core/` и `ports/`; проверять, что адаптеры не импортируют `core/` напрямую; хранить скриншоты запусков проверок.

**Критерии приёмки:** структура собирается `npm run build`, тесты проходят, CI-гарды действуют. Внешняя команда подтверждает, что `wrangler dev` запускается и `/healthz` отвечает 200.

### Майлстоун 3. Telegram Webhook & Messaging Adapter
**Цель:** приём обновлений Telegram, показ `typing…`, отправка ответов через адаптер, команды обрабатываются до ядра.

**Зависимости:** майлстоуны 1–2.

**Шаги и проверки:**
1. Telegram-парсер апдейтов и роут вебхука.
   - **Результат:** `http/telegram-webhook.ts` валидирует секрет, извлекает текстовые сообщения, команды `/admin*` направляет в фичи без обращения к ядру.
   - **Проверка:** юнит-тесты на разных видах апдейтов (`npm run test -- telegram-webhook`) и проверка, что неверный секрет отвергается; негативные тесты на отсутствующие поля и нестандартные апдейты.
   - **Статус:** ✅ Реализован парсер Telegram-апдейтов, команды `/admin*` перенаправляются в фичи, добавлены модульные тесты и расширен HTTP-роутер для раннего завершения запроса.
2. Messaging адаптер.
   - **Результат:** `adapters/telegram` реализует `MessagingPort` (`sendTyping`, `sendText`) через `fetch` Telegram API, учитывает chat_id и возможные ошибки API.
   - **Проверка:** мок-тесты адаптера, подтверждающие корректное формирование запросов и разбор `retry_after`; нагрузочный тест на серию из 10 сообщений выявляет троттлинг-ошибки.
   - **Статус:** ✅ Добавлен адаптер `createTelegramMessagingAdapter` с ретраями, очисткой текста и поддержкой `retry_after`; покрыт модульными тестами на happy-path и ошибки.
3. Конфигурация секретов и деплой.
   - **Внешние действия:**
     - В Wrangler: `wrangler secret put TELEGRAM_BOT_TOKEN`.
     - Настроить `TELEGRAM_WEBHOOK_SECRET` (для URL).
     - Установить вебхук командой `curl https://api.telegram.org/bot<TOKEN>/setWebhook` на `https://<worker>/webhook/<secret>`.
   - **Проверка:** после деплоя отправить тестовое сообщение, сообщить агенту payload и ответ; `wrangler tail` без ошибок; в журнале прогресса фиксируем время и результат ручного теста.
   - **Статус:** ✅ Конфигурация подтверждена: секреты `TELEGRAM_*` и `OPENAI_MODEL` заведены в разделе **Secrets**, `CONFIG_REQUIRED_SECRETS` подтягивается из Git (`wrangler.toml`). Деплои через Dashboard больше не очищают значения (`/admin/envz` → `openai_model: true`, см. `memory-bank/tests/2025-11-13-envz-after-secret.md`).
4. Typing-индикация.
   - **Результат:** воркер вызывает `sendTyping` до обращения к ядру и завершает после ответа; повторные сообщения не запускают параллельных `sendTyping`.
   - **Проверка:** в Telegram клиенте видна индикация typing перед ответом; подтверждение от команды, лог показывает корректные вызовы; автоматический тест имитирует долгий ответ ИИ и проверяет отключение typing.
   - **Статус:** ✅ Добавлен менеджер typing-индикации, `createTypingIndicator` запускает `sendTyping` до вызова ядра и предотвращает
     параллельные индикаторы; покрыто юнит-тестами (`typing-indicator.test.ts`, `router.test.ts`).
5. Устранение паузы между индикатором typing и ответом.
   - **Результат:** индикатор «Печатает…» либо остаётся активным до отправки сообщения, либо ответ уходит сразу после отключения индикатора, без визуальной паузы для пользователя.
   - **Проверка:** ручной тест в Telegram (по подтверждённому сценарию заказчика) фиксирует отсутствие длинной задержки между исчезновением индикатора и доставкой ответа; логи `sendTyping`/`sendText` содержат таймкоды без провалов.
   - **Статус:** ✅ Менеджер `createTypingIndicator` обновлён: `sendTyping` переотправляется с интервалом, пока не завершится обработка сообщения, добавлены автотесты на поддержание индикатора и остановку после ответа (`apps/worker-main/http/__tests__/typing-indicator.test.ts`). Фикс подтверждён ручным ретестом без паузы между индикатором и ответом ([memory-bank/tests/2025-11-15-typing-indicator-retest.md](memory-bank/tests/2025-11-15-typing-indicator-retest.md)) и закрытой задачей [typing-indicator-delay](memory-bank/issues/typing-indicator-delay.md).


**Риски и предохранители:** не логировать секреты; обработать команды `/start` и сервисные сообщения; мониторить `wrangler tail` на неожиданные типы апдейтов.

**Критерии приёмки:** вручную проверено, что сообщение из Telegram проходит через воркер, вызывает NOOP-AI и возвращает заглушечный ответ, а команды `/admin` не пробрасываются в ядро.

#### Текущая диагностика (М3)
1. **[Шаг 2] Ранний запуск тайпинга** — перестроить `DialogEngine.handleMessage`, чтобы `messaging.sendTyping` вызывался до операций D1; добавить тест на медленный storage. _Проверка 2.1: боевой тест + Cloudflare-лог (метрики задержки typing, ретраи D1, отсутствие fallback)._
   - [x] **Шаг 2.1. Тайпинг и D1 задержки.** Ранний тайпинг активен: индикатор появляется до старта вызова ИИ и отключается без залипаний; визуальная задержка <250 мс подтверждена замером тестового прогона. Лог симуляции фиксирует `storage.appendMessage` без ретраев и fallback, `d1_retry=0`/`storage_fallback=false`.
   - [x] ▶️ **ПРОВЕРКА после Шага 3 (2025-11-16).** Шаг 2 не требует уточнений: порядок `messaging.sendTyping` в `DialogEngine.handleMessage` сохранился сразу после rate-limit, тест `"запускает typing до завершения операций хранения"` подтверждает вызов до завершения D1-операций, дополнительных сценариев для покрытия не выявлено. Для боевого стенда осталось подтвердить Cloudflare-лог (<250 мс, `chatIdRawType=string`, отсутствие задержек от retry `sendText`).

### Майлстоун 4. OpenAI Responses Integration
**Цель:** подключить OpenAI Responses как источник ответов, используя REST `/v1/responses` без стриминга.

**Зависимости:** майлстоуны 1–3.

**Шаги и проверки:**
1. Адаптер `AiPort` для Responses.
   - **Результат:** `adapters/openai-responses` формирует запрос `POST https://api.openai.com/v1/responses` с явным `model`, контекстом из `input` и, при наличии, `prompt: { id: pmpt_…, variables }`; поддерживает `previous_response_id` и разбирает `output_text`/items.
   - **Проверка:** юнит-тесты с `fetch-mock` проверяют тело запроса (без `assistant_id`), обработку переменных промпта, таймаут и ретраи; `npm run test -- openai-adapter`; лог содержит `response_id`.
   - **Статус:** ✅ Миграция завершена: адаптер использует `model`/`prompt.id`, парсит `output_text` и `items`, восстанавливает `previous_response_id` из контекста, а тесты `apps/worker-main/adapters/openai-responses/__tests__/openai-responses.test.ts` подтверждают отсутствие `assistantId` и корректный разбор ответа.
   - **Обновление:** памятка [memory-bank/openai-responses-prompt.md](memory-bank/openai-responses-prompt.md) дополнена фактами по ретраям и `previous_response_id` (см. журнал 2025-10-27).
2. Конфигурация секретов.
   - **Внешние действия:**
     - `wrangler secret put OPENAI_API_KEY`.
     - `wrangler secret put OPENAI_MODEL`.
     - (опционально) `wrangler secret put OPENAI_PROMPT_ID`, `wrangler secret put OPENAI_PROMPT_VARIABLES`.
   - **Проверка:** пробный вызов `wrangler dev` с тестовым запросом; лог подтверждает использование `model` и корректный разбор `prompt.id`; секреты не попадают в stdout.
   - **Статус:** ✅ `OPENAI_API_KEY` и `OPENAI_MODEL` заведены в разделе **Secrets**, подтверждён ретест `/admin/envz` после деплоя (значения не сбрасываются). Plaintext-конфигурация (`CONFIG_REQUIRED_SECRETS` и др.) синхронизируется через `wrangler.toml`, поэтому она подтягивается из Git и не редактируется через UI. `OPENAI_PROMPT_VARIABLES` сейчас не используется; если понадобится, добавляем её в Git перед деплоем, чтобы значение не слетало.
3. Интеграция с ядром.
   - **Результат:** `DialogEngine` через порт получает ответ, передаёт `output_text` адаптеру Telegram, fallback остаётся прозрачным для ядра; `previous_response_id` сохраняется для последующих вызовов.
   - **Проверка:** E2E тест через `wrangler dev` с отправкой сообщения; лог содержит `response_id`, `previous_response_id` и усечённый текст ответа; тестовое окружение использует зашифрованные переменные.
   - **Статус:** ✅ Интеграция подтверждена: последовательность запросов через `wrangler dev` восстанавливает контекст и прокидывает `response_id`, что зафиксировано в протоколе [2025-11-10-d1-context-restore.md](memory-bank/tests/2025-11-10-d1-context-restore.md).
4. Обработка деградаций.
   - **Результат:** при ошибке Responses бот отправляет запасной ответ и логирует ошибку с requestId, ядро не падает.
   - **Проверка:** тест с симулированным `500` (мок) проверяет fallback; команда подтверждает, что Telegram ответ содержит дружелюбное сообщение об ошибке; метрика ошибок Responses инкрементируется с пометкой `ai_response_failure`.
   - **Статус:** ✅ Добавлены ретраи и fallback: деградации Responses покрыты автотестами и ручным сценарием ([memory-bank/tests/2025-11-15-openai-fallback.md](memory-bank/tests/2025-11-15-openai-fallback.md)), отдельный мониторинг Storage описан в [d1-storage-degradation.md](memory-bank/issues/d1-storage-degradation.md).

**Риски и предохранители:** таймаут ≤20 с на запрос Responses; не логировать персональные данные; мониторить количество повторов.

**Критерии приёмки:** бот отвечает реальным текстом от Responses, fallback работает, секреты заданы.

### Майлстоун 5. D1 Persistence & Диалоговая память
**Цель:** хранить пользователей и историю диалогов в D1, предоставлять короткий контекст ядру.

**Зависимости:** майлстоуны 1–4.

**Шаги и проверки:**
1. Схема базы и миграции.
   - **Результат:** SQL миграции для таблиц `users`, `messages` (user_id, role, content, timestamps, metadata).
   - **Проверка:** `wrangler d1 execute <DB> --file=...` создаёт таблицы без ошибок; локально проверен откат миграции.
   - **Внешние действия:** создать D1 binding `wrangler d1 create dialog-db`, привязать в `wrangler.toml` как `DB`.
   - **Статус:** ✅ Добавлена миграция `0001_init_dialog_tables.sql` с таблицами `users`, `messages` и индексами по пользователям
     и временным меткам для быстрых выборок контекста.
2. Адаптер `StoragePort` для D1.
   - **Результат:** CRUD-операции сохраняют пользователей и сообщения, `getRecentMessages` возвращает N последних реплик.
   - **Проверка:** тесты с `@cloudflare/workers-types` + `better-sqlite3` (локально) проверяют вставку/чтение и корректный порядок сообщений.
   - **Статус:** ✅ Реализован адаптер `createD1StorageAdapter`, добавлены модульные тесты `apps/worker-main/adapters/d1-storage/__tests__/d1-storage.test.ts`, покрывающие апдейт пользователя, дедупликацию сообщений и выборку истории.
3. Интеграция в композицию.
   - **Результат:** `compose.ts` подключает адаптер при наличии D1 binding, иначе NOOP.
   - **Проверка:** `npm run test -- composition-storage`; отрицательный тест без биндинга возвращает NOOP.
   - **Статус:** ✅ Проведена проверка реального биндинга `DB`: после `wrangler deploy` маршрут `/admin/envz` фиксирует `db_bound: true`, а `wrangler d1 execute DB --command "SELECT user_id, role FROM messages ORDER BY created_at DESC LIMIT 3"` возвращает ожидаемые строки без ошибок (см. протокол `memory-bank/tests/2025-11-10-d1-context-restore.md`).
4. Диалоговая память.
   - **Результат:** ядро запрашивает последние N сообщений и передаёт их Responses; сохраняет и исходное, и ответ.
   - **Проверка:** E2E прогон: отправить 3 сообщения, убедиться, что D1 содержит записи (команда запускает `wrangler d1 execute` SELECT и сообщает результат); проверка очистки старых записей при достижении лимита.
   - **Статус:** ✅ Подтверждено восстановление контекста: отправка последовательности `ping → second → third` возвращает ответы Responses с учётом истории, `getRecentMessages` выдаёт три последних сообщения, а повторный вызов `/admin/envz` после теста отражает неизменность биндингов; фиксация результатов и выборок приведена в `memory-bank/tests/2025-11-10-d1-context-restore.md`.

**Риски и предохранители:** план миграций без простоя; мониторинг размера базы; перед изменениями схемы выполняется бэкап.

**Критерии приёмки:** данные пишутся в D1, Responses получает контекст, бот отвечает, история доступна.

#### Текущая диагностика (М5)
1. **[Шаг 3] Целостность записи ответов** — откладывать запись `assistant` в D1 до успешной отправки; покрыть тестом с падением `sendText`. _Проверка 3.1: технический мок + боевой диалог (метрики согласованности истории и отсутствия fallback)._
   - [x] **Шаг 3.1. Целостность записи ответов (prod).** Проверка 3.1 — PASS: на выборке последних сообщений прод-диалога ассистентские записи появляются только после подтверждённой отправки и содержат `messageId`; дублей и записей без `messageId` не найдено; fallback не срабатывал. В Cloudflare-артефакте негативного прогона `sendText` ([лог отказа](memory-bank/logs/cloudflare-sendtext-failure-2025-11-16.log)) виден `[dialog-engine][sendText][error]`, запись `assistant` подавлена.
2. **[Шаг 4] Усиление D1-ретраев** — увеличить попытки и бэкофф с джиттером в `runWithRetry`, логировать деградации. _Проверка 4.1: нагрузочный техтест + лог (метрики успешных записей и времени ретраев)._
   - [x] **Шаг 4. Усиление D1-ретраев.** `runWithRetry` расширен до 6 попыток с экспоненциальным бэкоффом и джиттером, добавлены логи после ≥3 попыток и фильтр неретраябельных ошибок; юнит-тесты покрывают длинные цепочки ретраев и безретрайные ошибки. Шаг 4 выполнен. Экспоненциальные ретраи с джиттером включены, кейсы ≥3 попыток и `max_retries_exceeded` логируются. Под стрессом допускаются единичные `max_retries_exceeded`; в обычном режиме порог тревоги >1–2 %. D1-стресс прошёл, падения диалогов вызваны не D1, а таймаутами OpenAI (см. отчёт 2025-11-16).
   - [x] **Шаг 4.4. D1 стресс-ручка и AI queue конфиг.** Повторный стресс-тест Variant C завершён: `STRESS_TEST_ENABLED=1` удерживает `droppedSinceBoot=0`, а `/admin/diag?q=ai-queue` берёт значения из `AI_QUEUE_CONFIG` (источник = `kv`). Хронология:
     - 2025-11-17 — первый прогон зафиксировал `max_retries_exceeded` и `kvConfig:null` ([лог](memory-bank/logs/stress-test-2025-11-17-ai-queue.md), [diagnostics](memory-bank/diagnostics.md#diag-20251117)).
     - 2025-11-18 — smoke с обновлёнными ретраями прошёл без `max_retries_exceeded`, очередь осталась `active≤1` ([diagnostics](memory-bank/diagnostics.md#diag-20251118)).
     - 2025-11-19 — включён Variant C с повышенными таймаутами и полями `sources.*`; `/admin/diag?q=ai-queue` и `getQueueStats` возвращают `requestTimeoutMs=18000`, `retryMax=3`, `maxConcurrency=4`, `maxQueue=64` с пометкой `kv` ([diagnostics](memory-bank/diagnostics.md#diag-20251119), [REPORT-ai-throughput-20251116](reports/REPORT-ai-throughput-20251116.md)).

### Майлстоун 6. Rate Limit Feature
**Цель:** ограничивать количество ответов пользователю в сутки с помощью KV и промптового уведомления.

**Зависимости:** майлстоуны 1–5.

**Шаги и проверки:**
1. KV-хранилище и адаптер `RateLimitPort`.
   - **Результат:** реализация инкремента суточных счётчиков, TTL = 24ч.
   - **Проверка:** юнит-тесты с локальным KV-симулятором (`miniflare`) проверяют достижение лимита и гонки при параллельных запросах.
   - **Внешние действия:** создать KV namespace `wrangler kv:namespace create RATE_LIMIT`, привязать как `RATE_LIMIT_KV`.
   - **Статус:** ✅ Реализован адаптер `createKvRateLimitAdapter`, ключи сегментируются по окнам 24ч, покрыто тестами `apps/worker-main/adapters/kv-rate-limit/__tests__/kv-rate-limit.test.ts`.
2. Фича `features/limits`.
   - **Результат:** middleware блокирует ядро при превышении лимита, отправляет пользователю уведомление, логирует событие с ключевыми полями (`userId`, `limit`, `ttl`).
   - **Проверка:** интеграционный тест с имитацией нескольких запросов; `npm run test -- limits` и проверка логов; метрика отказов лимита доступна в аналитике.
   - **Статус:** ✅ Добавлен нотификатор `createRateLimitNotifier`, роутер вызывает его при ответе `'rate_limited'`, добавлены тесты `apps/worker-main/features/limits/__tests__/rate-limit-notifier.test.ts` и `apps/worker-main/http/__tests__/router.test.ts`.
3. Настройка и проверка лимитов сообщений на пользователя.
   - **Результат:** параметры суточного лимита (`RATE_LIMIT_DAILY_LIMIT`, `RATE_LIMIT_WINDOW_MS`) документированы; подтверждено, что при превышении лимита бот отправляет уведомление «🥶⌛️ <рассчитанный TTL>». На период тестов планируется повысить лимит и сократить окно по запросу заказчика.
   - **Проверка:** ручной сценарий фиксирует поведение до и после достижения лимита (лог в `memory-bank/tests/2025-11-13-rate-limit-check.md`). Следующий прогон должен дополнительно проверить таймер обратного отсчёта и автоматический сброс после окончания окна.
   - **Статус:** ✅ Повторный прогон подтвердил обновлённое уведомление без эмодзи-песочных часов и корректную блокировку на 51-м запросе ([memory-bank/tests/2025-11-13-rate-limit-check.md](memory-bank/tests/2025-11-13-rate-limit-check.md)). Дневной лимит и окно задокументированы, результат согласован с заказчиком.

**Риски и предохранители:** KV недоступен → разрешаем ответ и логируем предупреждение; не храним чувствительные данные; документируем процедуру очистки ключей.

**Критерии приёмки:** лимит блокирует ответы после N сообщений в сутки и легко отключается флагом.

#### Текущая диагностика (М6)
1. **[Шаг 6] Разделение лимитов пользователей и админов** — вернуть «сырые» rate-limit порты админским фичам, оставить toggle для ядра. _Проверка 6.1: интеграционный тест с `LIMITS_ENABLED=0` + боевой спам `/export` (метрики разрешённых/заблокированных запросов)._
   - [x] **Шаг 6 (2025-11-23).** `composeWorker` публикует `ports.rawRateLimit` для админских маршрутов, а `/export` переключён на этот порт, поэтому флаг `LIMITS_ENABLED=0` снимает ограничения только с пользовательских диалогов. Тест `apps/worker-main/composition/__tests__/compose.test.ts` воспроизводит сценарий `LIMITS_ENABLED=0` и проверяет, что админский порт остаётся жёстким. **ПРОВЕРКА 6.1**: при любом релизе отключайте лимиты через KV и фиксируйте пару метрик — отсутствие 429 для обычного диалога и сохранение 429 на серии `/export`.
     - **Статус 2025-11-23:** боевой прогон `/export` при `LIMITS_ENABLED=0` подтвердил отсутствие 429 на пользовательских сообщениях и срабатывание `admin export cooldown active` + HTTP 429 на повторных `/export` в течение 60 с. Подтверждение: `logs/limits-6-1-tail.json`, `reports/REPORT-limits-export-cooldown-20251123.md`.

### Майлстоун 7. Export CSV Feature
**Цель:** выгружать диалоги из D1 в CSV по запросу администратора.

**Зависимости:** майлстоуны 1–6.

**Шаги и проверки:**
1. HTTP-роут `/admin/export`.
   - **Результат:** принимает диапазон дат/пагинацию, проверяет `X-Admin-Token`.
   - **Проверка:** юнит-тесты авторизации и валидации входа.
   - **Статус:** ✅ Добавлен конструктор роута `createAdminExportRoute`, проверки реализованы в `apps/worker-main/features/export/__tests__/admin-export-route.test.ts` и интегрированы в HTTP-роутер.
2. CSV-генератор.
   - **Результат:** потоковая генерация CSV (chunked) поверх выборки D1, избегая больших ответов и обеспечивая корректный BOM для Excel.
   - **Проверка:** тест на генерацию >100 записей, проверка памяти (отсутствие утечек) и корректности заголовков; проверяем защиту от CSV-инъекций.
   - **Статус:** ✅ Реализован обработчик `createCsvExportHandler` с потоковой выдачей CSV, BOM и защитой от CSV-инъекций; покрыт тестами `apps/worker-main/features/export/__tests__/csv-export.test.ts`.
3. Внешняя проверка.
   - **Внешние действия:** команда выполняет `curl -H "X-Admin-Token: ..." https://<worker>/admin/export?from=...` и подтверждает получение корректного CSV; проверяется заголовок `Content-Type: text/csv; charset=utf-8`.
   - **Статус:** ✅ 2025-11-08 повторный прогон `/admin export`, `/admin export 2025-11-01` и `/admin export 2025-11-01 2025-11-04` на продовом воркере подтвердил корректные ответы и формирование CSV, секрет `ADMIN_EXPORT_TOKEN` и лог `ADMIN_EXPORT_LOG` восстановлены.
4. Жёсткие проверки окружения и усиление self-test.
  - **Шаг 4.1 — жёсткая проверка OpenAI env.**
    - **Результат:** [`apps/worker-main/index.ts`](apps/worker-main/index.ts) валидирует обязательные переменные (`OPENAI_API_KEY`, `OPENAI_MODEL`, опционально `OPENAI_PROMPT_ID`) с fail-fast логикой; при отсутствии ключей воркер завершает и пишет понятное сообщение в лог.
    - **Проверка:** ревью кода подтверждает наличие проверки и fail-fast поведения; финальный чек `npm test -- --run apps/worker-main/features/admin-diagnostics/__tests__/self-test-route.test.ts`.
  - **Шаг 4.2 — жёсткая проверка Telegram токена.**
    - **Результат:** [`apps/worker-main/index.ts`](apps/worker-main/index.ts) дополнительно проверяет, что `TELEGRAM_BOT_TOKEN` непустой (префикс `bot` добавляется адаптером [`apps/worker-main/adapters/telegram/index.ts`](apps/worker-main/adapters/telegram/index.ts)), ошибки логируются и блокируют запуск.
    - **Проверка:** ревью [`apps/worker-main/index.ts`](apps/worker-main/index.ts) подтверждает проверку токена на непустое значение; финальный чек `npm test -- --run apps/worker-main/features/admin-diagnostics/__tests__/self-test-route.test.ts`.
  - **Шаг 4.3 — усиление `/admin/selftest`.**
    - **Результат:** [`apps/worker-main/features/admin-diagnostics/self-test-route.ts`](apps/worker-main/features/admin-diagnostics/self-test-route.ts) выполняет цепочку проверок (OpenAI → Telegram) и возвращает структурированный отчёт с полями `openAiOk`, `telegramOk`, `errors[]` и метриками задержек.
    - **Расширение:** тот же маршрут обрабатывает `q=utm`, вызывая `StoragePort.saveUser/getRecentMessages` и подтверждая доступность колонки `utm_source` (ответ `200` при `utmDegraded = false`).
    - **Новая диагностика:** [`apps/worker-main/features/admin-diagnostics/bindings-route.ts`](apps/worker-main/features/admin-diagnostics/bindings-route.ts) добавляет `/admin/diag?q=bindings`, выводя безопасные биндинги для `saveUser` и `getRecentMessages`.
  - **Проверка:** добавлены юнит-тесты для маршрута; финальный чек `npm test -- --run apps/worker-main/features/admin-diagnostics/__tests__/self-test-route.test.ts` подтверждает корректность.
  - **Статус:** ✅ Шаг подтверждён: self-test успешно обрабатывает no-op AI, отчёт формирует ошибки без падений ([commit dd01f6e](https://github.com/egormob/tg-responcer/commit/dd01f6eb8acff15e0b88ed906c19200a409315eb), [лог `npm run check:roadmap`](logs/check-roadmap.log)). 2025-11-09 дополнительно выполнен self-test `q=utm` (ответы `200` с `utmDegraded = false`), артефакты сохранены в [memory-bank/tests/2025-11-09-utm-selftest.md](memory-bank/tests/2025-11-09-utm-selftest.md).
5. Шаг 5 — внешняя проверка `/admin/export` на боевом воркере.
   - **Условие запуска:** стартуем только после закрытия gate-чека М7 и синхронизации с задачей [#458](memory-bank/issues/m7-parallel-work.md); работа ведётся в ветке `work/export-external-check` c обязательным `git rebase origin/work` перед любым пушем.
   - **Результат:** команда с доступом к продовому воркеру выполняет реальный запрос `GET https://<prod-worker>/admin/export` с валидными параметрами и подтверждает получение CSV с корректными заголовками, содержимым и ограничениями по диапазонам.
   - **Проверка:** зафиксирован протокол внешней проверки (скрин/лог `curl`, отметка токена, параметры запроса, контрольная проверка Excel) в `memory-bank/tests/2025-11-07-admin-export-prod.md`; ссылка на выгруженный CSV хранится в `logs/2025-11-07-admin-export-prod.csv`.
  - **Статус:** ✅ 2025-11-08 повторное тестирование подтвердило корректный ответ `/admin/export` на продовом воркере для полных и диапазонных запросов, журнал `ADMIN_EXPORT_LOG` фиксирует выгрузки.
6. Шаг 6 — синхронизация памяти и журналов после ретестов.
   - **Условие запуска:** стартуем сразу после успешного закрытия Шага 5 и подтверждения gate-чека; исполнение привязано к issue [#459](memory-bank/issues/m7-parallel-work.md) и ведётся в ветке `work/memory-sync` с предварительным `git rebase origin/work`.
   - **Результат:** обновлены `memory-bank/` (журналы проверок, контрольные CSV, памятки о деплое) и `RoadMap.md` актуальными ссылками на продовые артефакты; фиксируется, что локальные журналы и продовые проверки синхронизированы.
   - **Проверка:** ревью `memory-bank/` подтверждает наличие свежих артефактов, `npm run check:roadmap` проходит без замечаний, в журнале прогресса отмечены переносы статусов из ожидания в `✅` и ссылки на протоколы ретестов.
 - **Статус:** ✅ Статус перенесён со статуса ожидания после публикации артефактов и обновления журнала в рамках issue [#459](memory-bank/issues/m7-parallel-work.md); ветка `work/memory-sync` смержена `Rebase and merge`.

7. Шаг 7 — Telegram-команда `/export` и унификация админ-доступа.
   - **Результат:**
     - Создан модуль админ-идентификации (`features/admin-access`), который читает список Telegram `user_id` из биндинга `ADMIN_TG_IDS` (Cloudflare KV, ключ `whitelist`, значение — JSON-массив строк). Модуль используется и для команд `/export`, и для Broadcast, исключая жёстко заданные значения.
     - Команда `/export [from] [to]` в чате с ботом доступна только whitelisted администраторам: без дат выгружает все записи, с одной датой — диапазон `[from, now]`, с двумя — `[from, to]` (ISO `YYYY-MM-DD`). Дополнительно команда `/admin` → `status` возвращает `admin-ok`/`forbidden`, чтобы мгновенно проверить актуальность whitelist.
    - Добавлен rate-limit 1 запрос / 60 секунд на `user_id` (KV TTL), общие ограничения логируются.
     - Каждая выгрузка журналируется в KV (`ADMIN_EXPORT_LOG`): фиксируются `user_id`, аргументы, количество строк и timestamp, TTL — 30 дней.
   - **Проверка:**
     - Юнит-тесты для модуля админ-доступа (валидные/невалидные ID, несколько ID в списке, пустой список) и обработчика команды (`/export`, варианты аргументов, отказ для не-админа, соблюдение rate-limit).
     - Интеграционный тест webhook → команда `/export` → генерация CSV + логирование.
    - Ручной прогон: добавить свой `user_id` в KV, выполнить `/export 2025-11-01 2025-11-04`, получить CSV, проверить запись в журнале, убедиться, что повторная команда в течение 60 секунд отклоняется.
   - **Внешние действия:** до старта шага создать KV namespace `ADMIN_TG_IDS`, привязать как биндинг `ADMIN_TG_IDS` и записать значение `{"whitelist": ["<id1>", "<id2>"]}` через `wrangler kv:key put`. Журнал выгрузок хранить в отдельном namespace `ADMIN_EXPORT_LOG` (TTL 30 дней). После релиза команда с правами администратора должна актуализировать списки ID.
   - **Статус:** ✅ Ручной прогон 2025-11-09 подтвердил, что whitelisted администратор получает CSV через `/export`, записи фиксируются в `ADMIN_EXPORT_LOG`, а rate-limit и проверка доступа срабатывают. Пункт ранее оставался без отметки, потому что ожидали подтверждённого отчёта по унификации whitelista и журнала.

8. Шаг 8 — UTM-метки и справочник deeplink.
   - **Результат:** подготовлена справка по deeplink/`startapp` и ограничениям 64 символов (`memory-bank/references/telegram-deeplink-utm.md`), обновлена операционная памятка экспорта (`memory-bank/operations/export.md`) с фактической схемой CSV и инструкцией по проверке `users.utm_source`.
   - **Проверка:** ревью `RoadMap.md` и памяток подтверждает описание текущего формата (`message_metadata` без `utm_*`) и наличие инструкции по ручной проверке `wrangler d1 execute ... SELECT user_id, utm_source ...`; журнал прогресса содержит запись о запуске шага и результатах документирования.
  - **Статус:** ✅ Документация по UTM и экспорту актуализирована; для автоматического экспорта UTM создан фоллоу-ап [issue](memory-bank/issues/m7-export-utm-gap.md) с кнопкой быстрого старта (`npm run task:checkout m7-export-utm-gap`). 2025-11-09 обновлены `parseStartPayload` и тесты (`telegram-webhook`, `utm-tracking`) с подтверждением через `npx vitest run ...` сохранения регистра и поддержки `src.`-payload. 2025-11-10 дополнительно зафиксирован сценарий mini app (`startapp`, `initData → start_param`) с автотестами `apps/worker-main/http/__tests__/telegram-webhook.test.ts` и `apps/worker-main/features/utm-tracking/__tests__/create-telegram-webhook-handler.test.ts`. 2025-11-11 продублирован чек цепочки `/start src_TEST` → `StoragePort.saveUser` → `knownUsersCache` → `/admin/export`: `apps/worker-main/adapters/d1-storage/__tests__/d1-storage.test.ts` подтверждает запись `utm_source` после восстановления схемы, а `memory-bank/operations/export.md`/`docs/operations/diagnostics.md` дополнились инструкцией по ручной проверке (`wrangler d1 execute ...`, `wrangler tail`, `/admin/known-users/clear`).


**Риски и предохранители:** ограничение выгрузки пагинацией и валидацией диапазонов; токен не логируется; большая выгрузка переводится в очередь.

**Критерии приёмки:** админ может скачать CSV, формат совместим с Excel, запрос защищён токеном.

#### Текущая диагностика (М7)
1. **[Шаг 5] Полный экспорт с пагинацией и UTM** — итерировать `x-next-cursor`, собирать полный CSV и предупреждать о лимите строк. _Проверка 5.1: боевой `/export` + ревизия CSV (метрики числа строк и UTM)._
  - [x] **Шаг 5.1. Гарантированная запись UTM при регистрации (2025-11-20).** Ручной прогон `/start src_TEST-CAMPAIGN` → «ping» подтвердил, что `utm_source` записывается в `users` без деградации, повторные сообщения без payload не вызывают `saveUser`, а `knownUsersCache` удерживает исходную метку. Очередь AI (Variant C) читает лимиты из `AI_CONTROL_KV` и переживает таймауты OpenAI через `⚠️ → 🔁💬` без влияния на UTM. Метрики и логи сведены в журнале [2025-11-20](memory-bank/diagnostics.md#diag-20251120).
  - [x] **Шаг 5.2. Стабильный `/start` без вызова AI (2025-11-21).** Приветствие переведено в локальную обработку: `/start` всегда выполняет только UTM-парсинг, `saveUser` и обновление `knownUsersCache`, не создавая запросов в OpenAI. Приветствие захардкожено («Привет, <Имя>!» / «Привет!»), fallback «⚠️ → 🔁💬» больше не появляется при первом сообщении. Памятка [`memory-bank/operations/start-command.md`](memory-bank/operations/start-command.md) и журнал [2025-11-21](memory-bank/diagnostics.md#diag-20251121) фиксируют tail-метрики вместо прямой ссылки на артефакт.
  - [x] **Шаг 5.3. Полный экспорт с пагинацией (2025-11-22).** `/export` итерирует `x-next-cursor`, собирает до 5 000 строк (батчи по 1 000) и объединяет UTM-источники из всех страниц. Бот уведомляет администратора о пустой выгрузке («CSV содержит только заголовок») или обрезке («⚠️ Экспорт ограничен первыми 5000 строками…»), а интеграционный тест проверяет склейку нескольких CSV-страниц. Добавлена операционная **ПРОВЕРКА «Полнота экспорта»**: боевой `/export` → сверка числа строк и UTM для всех пользователей, запись в журнале [2025-11-22](memory-bank/diagnostics.md#diag-20251122).
   - [x] **Следующий шаг блока 5.** CSV с пагинацией используется в BI-отчёте по UTM: аналитическая выкладка ([reports/REPORT-utm-tracking-20251120.md](reports/REPORT-utm-tracking-20251120.md)) и охлаждающий отчёт по лимитам рассылок ([reports/REPORT-limits-export-cooldown-20251123.md](reports/REPORT-limits-export-cooldown-20251123.md)) зафиксированы, RoadMap и `memory-bank/operations/export.md` обновлены.

### Майлстоун 8. Broadcast Feature
**Цель:** минимальная модель ручной рассылки сообщений администраторами напрямую из Telegram.

**Зависимости:** майлстоуны 1–7.

- **Gate-чек:**
  - **Статус:** ✅ Gate-чек закрыт (см. запись журнала от 2025-11-06); М8 разблокирован для продолжения работ.
  - **Примечание:** `npm run check:roadmap` завершён успешно, лог хранится вне репозитория в `logs/check-roadmap.log`.

**Шаги и проверки:**
1. Очередь и команда `/broadcast` с мгновенной отправкой.
    - **Результат:** whitelisted администратор вызывает `/broadcast`, бот запрашивает аудиторию (`/everybody` или список user_id/username с дедупликацией), затем просит текст для указанного числа получателей. Очередь отправки и админ-роут под флагом `BROADCAST_ENABLED` собраны 2025-10-29; HTTP-роуты, дополнительные очереди и сегментация не используются.
    - **Проверка:** модульные тесты покрывают сценарии «нет доступа», «пустой текст», «длинный текст >3970 символов», `not_found` в аудитории, `/cancel` и успешную отправку; ручной прогон подтверждает доставку на хотя бы один distinct `chat_id` из D1.
   - **Статус:** ✅ Шаг М8.Ш1 закрыт. Актуальный UX и критерии успеха задокументированы в памятке [`memory-bank/operations/broadcast-operations.md`](memory-bank/operations/broadcast-operations.md#памятка-минимальной-рассылки): (1) текст проходит 1000 мс дебаунс и отклоняется при нескольких сообщениях, (2) запуск рассылки происходит только после `/send`, а `/new_text` и `/cancel_broadcast` доступны всегда, (3) после `/cancel_broadcast` pending-сессия очищается, а чат возвращается в стандартное меню. Tail [`diag-2025-11-30-broadcast-webhook.md`](memory-bank/logs/diag-2025-11-30-broadcast-webhook.md#tail-успешного-webhook-и-broadcast) подтверждает `/broadcast → /send` без регрессий.
2. Операционная памятка и ограничения.
    - **Результат:** обновлены `memory-bank/operations/broadcast-operations.md`, `README.md` и справка по Telegram-админам (`docs/telegram-admin-commands.md`) с описанием двухшагового UX («/broadcast → /everybody или список → Шаг 2: текст → мгновенная отправка»), требованием whitelisting, distinct `chat_id` из D1 и логами `broadcast_resolve`/`broadcast_summary` + KV `broadcast:last`.
    - **Проверка:** ревью документов показывает отсутствие устаревших инструкций; требования к правам администратора, источнику получателей (D1 без `BROADCAST_RECIPIENTS`), длине сообщения и порядку запуска (после шага очередей, до финального ревью) отражены во всех связанных памятках; ссылки ведут только на доступные шаги.
   - **Статус:** ✅ Памятки уточнены: добавлены правила хранения pending-сессий, порядок запуска задачи и чек-лист приёмки отправки.
    - **Guardrails:** команда `/broadcast` поддерживает только два режима (все из D1 или явный список); любые попытки вызвать устаревшие подпотоки (`/broadcast queue`, `/broadcast http`, `/admin broadcast queue` и др.) приводят к явному отказу; при срабатывании rate limit администратор всегда получает ответ с указанием задержки.
3. Контур наблюдения за минимальной моделью.
   - **Статус:** ✅ Tail и журналы синхронизированы: `memory-bank/logs/wrangler-tail-admin-2025-11-26.md`, `memory-bank/logs/telegram-admin-recovery-2025-11-26.md`, `memory-bank/logs/m9-ai-export-timeout-2025-11-26.md` и свежий [`memory-bank/logs/diag-2025-11-30-broadcast-webhook.md`](memory-bank/logs/diag-2025-11-30-broadcast-webhook.md#tail-успешного-webhook-и-broadcast) показывают цепочку `/admin → /broadcast → /admin status` без 429 и с отчётами по очереди AI.
   - **Результат:** базовый чек-лист мониторинга (tail + whitelist + журнал рассылок) определён и задокументирован; whitelisted ID подтверждены.
   - **Критерий приёмки:** каждое использование `/broadcast` фиксируется в журнале и tail без ошибок, администратор получает резюме отправки и снимок очереди.
4. Итоговая проверка минимальной рассылки.
   - **Результат:** подтверждена доставка по D1-аудитории с заполненным списком получателей, без сегментации и фильтров; tail содержит `broadcast recipients resolved (source=d1, recipients>0)` и `broadcast pool completed`.
   - **Проверка:** повторить `/broadcast` с whitelisted аккаунта, сверить доставку на выборке D1 ≥N получателей и убедиться в наличии KV-лога pending-сессии. Отчёт и хвосты фиксируются в [`memory-bank/logs/diag-2025-11-30-broadcast-webhook.md`](memory-bank/logs/diag-2025-11-30-broadcast-webhook.md#план-проверки).
   - **Статус:** ✅ 2025-11-18 лог `tg-responcer-broadcast-20251118-104723.log` подтверждает режимы `list` (1 адресат) и `all` (5 адресатов) с `delivered=recipients`, `failed=0`, `throttled429=0` и параллельным диалогом без роста AI-очереди. См. [`memory-bank/logs/broadcast-parallel-dialog-2025-11-18.md`](memory-bank/logs/broadcast-parallel-dialog-2025-11-18.md) и запись [diag-20251118b](memory-bank/diagnostics.md#diag-20251118b).
5. Guardrail: чекпоинты и авто-возобновление (наблюдаемость).
   - **Результат:** пул рассылки сохраняет чекпоинт (`jobId`, курсор аудитории, `delivered/failed/throttled429`, `durationMs`) и блокирует параллельный старт, если активная задача ещё не завершена. При паузе оператор видит карточку «Рассылка приостановлена» с текущим прогрессом.
   - **Проверка:** `/admin/diag?q=broadcast` показывает сохранённый чекпоинт и активный `jobId`; после рестарта воркера или явной паузы команда `/broadcast_resume` возобновляет отправку с того же курсора без пересборки аудитории и без двойных доставок.
   - **Статус:** ✅ Чекпоинты `broadcast:progress:*` сохраняют TTL 24 ч с `reason`, курсором и лимитером; уведомления и `/admin/diag?q=broadcast` показывают активный/paused `jobId`, remaining/total, TTL и команды «Продолжить/Отменить». Интеграционные тесты `broadcast-diag-route`, `minimal-broadcast-service` (pause/resume по `retry_after`, аварийная остановка) и `telegram-broadcast-command` блокируют параллельный старт.
- **Шаг М8.Ш3 — внедрить дебаунс-шлагбаум и подтверждение отправки.** [Памятка](memory-bank/operations/broadcast-operations.md#шлагбаум-текста) описывает UX из двух подшагов: «Шаг А — текст» (окно 1000 мс, правило «Telegram → несколько сообщений ⇒ отказ», ошибка «Эти данные пришли несколькими сообщениями…») и «Шаг Б — подтверждение» (обязательное `/send`, опции `/new_text` и `/cancel_broadcast`). Требование: без `/send` рассылка не стартует, а все отказанные попытки логируются как `text-gate_rejected`.
  - **Статус:** ✅ Восстановлен коммит `f0bfa1b02a8b84227ad5d2d21908573334e7763f` (PR #306) с реализованным дебаунсом текста, обязательным `/send` и командой `/cancel_broadcast`. Боевой тест подтвердил, что триггер текста и подтверждение отправки работают как запланировано.
- **Шаг М8.Ш4 — убрать дублирование предупреждения при превышении лимита текста.** При вводе сообщения длиннее лимита Telegram бот дважды выводит подсказку «Текст не укладывается в лимит…». Требуется воспроизвести, выяснить источник двойного ответа и вернуть единичное уведомление перед закрытием майлстоуна рассылки.
  - **Статус:** ✅ Дублирование предупреждения устранено; проверочный запуск `/broadcast` с текстом за пределами лимита выдаёт единственное уведомление и продолжает сценарий без повторов. Рассылка отправляется стабильно после подтверждения `/send`.
- **Дорожная карта UX рассылки** — ⏳ открыть и наполнить планом улучшений UX для `/broadcast` (подсказки, удержание сессий, фидбек администраторам); оставить незакрытым до согласования сценариев.
**Риски и предохранители:** запрет пустых сообщений; соблюдение лимита 3970 видимых символов (текущий программный порог); whitelisting администраторов в `ADMIN_TG_IDS`; ручной мониторинг ошибок в `wrangler tail`.

**Критерии приёмки:** рассылка запускается одной командой `/broadcast`, сообщение отправляется немедленно, документы отражают минимальную модель без очередей и HTTP API.

#### Текущая диагностика (М8)
1. **[Шаг 7] Троттлинг рассылок** — ограничить пул отправок и ретраи 429 в `minimal-broadcast-service`. _Проверка 7.1: техтест ≥10 получателей + лог (метрики пула, 429, длительности)._ 
   - [x] **Шаг 7. Троттлинг рассылок (2025-11-24).** `minimal-broadcast-service` использует управляемый пул с конфигурируемой конкуренцией, джиттером и ретраями 429 по `retry_after`. Юнит-тест покрывает ≥10 получателей и фиксирует, что `broadcast throttled` и `broadcast pool completed` логируют размер пула, число 429 и длительность. **ПРОВЕРКА 7.1** — PASS: тест `apps/worker-main/features/broadcast/__tests__/minimal-broadcast-service.test.ts` подтверждён, CF-лог фиксирует `poolSize`, `throttled429` и длительность выполнения.
2. **[Шаг 8] Вебхук Telegram** — бот не показывает typing до восстановления `TELEGRAM_WEBHOOK_SECRET`. _Проверка 8.1: `wrangler secret list` видит секрет, webhook `https://<worker>/webhook/<secret>` выставлен через Bot API._
 - [ ] **Регрессия 27.11.2025.** Любой `POST /webhook/*` возвращает `500 Webhook secret is not configured`/`403 Forbidden`, потому что секрет не провизионится. CLI `wrangler` из песочницы недоступен (`npm error 403`), поэтому подготовлен скрипт [`scripts/diagnose-telegram-webhook.sh`](scripts/diagnose-telegram-webhook.sh) и журнал [`memory-bank/logs/telegram-webhook-diagnostics-2025-11-27.md`](memory-bank/logs/telegram-webhook-diagnostics-2025-11-27.md) с инструкциями для оператора. После появления доступа необходимо загрузить секрет, перезаписать webhook и задеплоить воркер.
  - **Блокер №1 (ReferenceError createBroadcastDiagRoute, 27.11.2025).** `create-admin-router` падает при инициализации, потому что `apps/worker-main/features/index.ts` не экспортирует `createBroadcastDiagRoute`. Зависимости: (1) исправить barrel-файл и добавить недостающий экспорт; (2) пересобрать воркер и ретестировать `/admin/diag?q=broadcast` + `scripts/diagnose-telegram-webhook.sh`/`getWebhookInfo`; (3) зафиксировать результат в [`memory-bank/logs/diag-2025-11-27-broadcast-admin.md`](memory-bank/logs/diag-2025-11-27-broadcast-admin.md). Пока блокер не закрыт, `/webhook` отвечает 500 и `pending_update_count` растёт.
  - **Smoke 2025-11-16.** Webhook и диалог отвечают 200, `/broadcast` подтверждает отправку, но доставки нет из-за пустых получателей/фильтров. Лог: [`memory-bank/logs/telegram-webhook-smoke-2025-11-16.md`](memory-bank/logs/telegram-webhook-smoke-2025-11-16.md). Требуется заполнить аудиторию и повторить tail.
3. **Диагностика недоставленных рассылок (2025-11-16).** Добавлены логи источников получателей из D1 и fallback, выборки аудитории и статусы `delivered/failed`; лимитер очереди сигнализирует о бэклоге. Отчёт: [`memory-bank/logs/diag-2025-11-16-broadcast-undelivered.md`](memory-bank/logs/diag-2025-11-16-broadcast-undelivered.md). Точка контроля М8: убедиться, что `all` не даёт пустых списков и не блокирует диалоговые ответы. **Статус: ✅** закрыто логом `tg-responcer-broadcast-20251118-104723.log` ([diag-20251118b](memory-bank/diagnostics.md#diag-20251118b)): `mode=list` и `mode=all` доставляют всем адресатам (`delivered=recipients`, `failed=0`), очередь AI остаётся пустой (`queued=0`, `active≤1`), параллельный пользовательский диалог стабилен.

### Промежуточный майлстоун: Фиксы и предохранители
**Цель:** собрать мелкие исправления и защитные механизмы перед финальными тестами и перенесёнными проверками.

**Шаги и проверки:**
- **Шаг F1 — обработка неподдерживаемых форматов.** При получении ботом данных, которые он не может обработать (например, голос вместо текста или текст вместо списка получателей), выводить явный ответ «Я не понимаю этот формат.» без перехода в нестабильное состояние. _Статус: ⏳ запланировано._
- **Шаг F2 — перенос нагрузочного теста рассылки.** Задачу «Нагрузочный тест рассылки» сдвинуть ближе к блоку «Майлстоун D — Повторный аудит после шагов» (Шаг 9 «Смягчение self-test OpenAI»), чтобы запускать её после завершения предохранителей и актуальных фиксов. _Статус: ⏳ ожидает позиции в графике._
- **Шаг F3 — пакет будущих предохранителей.** Дополнительные задачи пользователь предоставит по ходу работы; фиксировать их в рамках этого майлстоуна и согласовывать перед выполнением. _Статус: ⏳ задачи будут добавляться._

**Критерии приёмки:** все запросы неподдерживаемых форматов получают явный отказ, список предохранителей зафиксирован и согласован, нагрузочный тест рассылки перенесён в блок финальных проверок без нарушения линейности RoadMap.

### Майлстоун 9. Observability & Operations
**Цель:** обеспечить мониторинг, алерты и устойчивость к сбоям.

**Зависимости:** майлстоуны 1–8.

**Шаги и проверки:**
1. Логирование и трассировка.
   - **Результат:** единый логгер в `shared/` с уровнями (info/warn/error) и контекстом (requestId, userId); интеграция с `wrangler tail`.
   - **Проверка:** `npm run test -- logger` и ручная проверка логов; персональные данные маскируются.
2. Метрики и алерты.
   - **Результат:** экспорт технических метрик (ответы, ошибки, латентность) в Workers Analytics Engine или сторонний сервис; определены алерты по ключевым SLA.
   - **Проверка:** отчёт команды о появлении метрик в панели; автоматический smoke-тест подтверждает отправку метрик; алерты тестируются через понижение порога.
3. Операционные инструкции.
   - **Результат:** документы в `memory-bank/` с инструкциями по обновлению ассистента, ротации секретов, процедурам инцидентов; указаны контакты on-call и частота ревизии.
   - **Проверка:** ревью содержимого, подтверждение команды; чек-лист подписан ответственными лицами.
4. Контур AI backpressure (вариант A).
   - **Контекст:** см. отчёт [REPORT-ai-throughput-20251116](reports/REPORT-ai-throughput-20251116.md); выбран вариант A (локальный семафор + очередь, конфиг из KV→Env→дефолт).
   - **Шаг 4.1 — конфигурация KV/Env.**
     - **Результат:** воркер читает `AI_MAX_CONCURRENCY`, `AI_QUEUE_MAX_SIZE`, `AI_TIMEOUT_MS`, `AI_RETRY_MAX` с приоритетом KV (`AI_QUEUE_CONFIG`) → Env → дефолты (4 / 64 / 12000 / 2); логи `[ai][config]` подтверждают источник значений.
     - **Проверка:** ревью конфиг-цепочки + ручной запуск `/admin/diag?q=ai-queue` после смены источника (KV → Env) фиксирует обновлённые параметры.
     - **Статус:** ✅ Завершено — значения 4 / 64 / 12000 / 2 активны на проде, конфиг смены источника задокументирован.
   - **Шаг 4.2 — семафор и очередь.**
     - **Результат:** глобальный семафор ограничивает одновременные fetch’и до `AI_MAX_CONCURRENCY`, очередь отклоняет переполнения дружелюбным сообщением.
     - **Проверка:** нагрузочный скрипт (12 параллельных запросов) подтверждает `active ≤ 4`, `queued ≤ 7`, отказ с текстом «Перегружены запросами 😔…» при превышении лимита.
     - **Статус:** ✅ Завершено — переполнение фиксируется логами `[ai][dropped]`, webhook отвечает 200 с дружелюбным отказом.
   - **Шаг 4.3 — таймауты, ретраи и логи.**
     - **Результат:** `AI_TIMEOUT_MS = 12000` с AbortController, `AI_RETRY_MAX = 2` с экспоненциальным backoff и логами `[ai][retry|timeout]`.
     - **Проверка:** имитация ответа 502 → один ретрай с успехом, таймаут принудительно срабатывает на 13 000 мс и логирует `[ai][timeout]`.
     - **Статус:** ✅ Завершено — логи `[ai][retry]` и `[ai][timeout]` отражают параметры, повторно проверены в smoke.
   - **Шаг 4.4 — диагностика `/admin/diag?q=ai-queue`.**
     - **Результат:** маршрут возвращает `status`, `maxConcurrency`, `maxQueue`, `active`, `queued`, `droppedSinceBoot`, `lastDropAt`; тревоги: Warning (`queued ≥ 48` >30 с), Critical (`droppedSinceBoot > 0` или `active == maxConcurrency` 60 с вместе с `queued == maxQueue`).
     - **Проверка:** ручной вызов до/после нагрузки, сравнение со счётчиками семафора.
     - **Статус:** ✅ Завершено — после smoke `active: 0`, `queued: 0`, `droppedSinceBoot: 0`.
   - **Шаг 4.5 — smoke-прогон с нагрузкой.**
     - **Результат:** сценарий и артефакты сведены в журнале [2025-11-16](memory-bank/diagnostics.md#diag-20251116) (`memory-bank/logs/ai-queue-smoke-2025-11-16.md` + diag JSON); повторный анализ подтвердил отсутствие `droppedSinceBoot` и источник `kv`.
     - **Проверка:** скрипт нагрузки и диаг-снимок приложены к [REPORT-ai-throughput-20251116](reports/REPORT-ai-throughput-20251116.md); smoke признан валидным для запуска следующего шага.
     - **Статус:** ✅ Вариант C выдерживает нагрузку, `droppedSinceBoot=0`.
   - ✅ **Шаг 4.x — gate-роллаут AI backpressure.**
     - **Gate 4.x.a — внутренняя синхронизация.** RoadMap и `memory-bank/tasks/m8-ai-backpressure.md` обновлены, `npm run check:roadmap` отражён в `logs/check-roadmap.log`, ссылки на отчёт [REPORT-ai-throughput-20251116](reports/REPORT-ai-throughput-20251116.md) добавлены.
       - **Статус:** ✅ Документы синхронизированы, чек пройден.
   - **Gate 4.x.b — внешние действия и артефакты.** Повторный smoke и tail сохранены (`memory-bank/logs/ai-queue-smoke-2025-11-16.md`, `memory-bank/logs/m9-ai-export-timeout-2025-11-26.md`), `/admin/diag?q=ai-queue` доступен из ответа `/admin status`.
     - **Статус:** ✅ Артефакты опубликованы и связаны с RoadMap; переход к шагу 5 разрешён.
     - **Внешние действия:** нагрузочный smoke (12 параллельных диалогов), выгрузка `logs/ai-queue-smoke-2025-11-16.log` и `logs/ai-queue-smoke-2025-11-16-diag.json` во внешнее хранилище, подтверждение доступности отчёта `REPORT-ai-throughput-20251116` для команды.
5. **Задача M9.X — интеграционная проверка связки «роли+команды» и AI-очереди с failover.**
   - **Суть:** прогнать сценарий «админские системные команды + очередь к OpenAI + фолловер» и зафиксировать инварианты: (а) `admin/user` команды не попадают в диалог с моделью; (б) повторные `/export` с кулдауном и rate-limit не вызывают `AI_QUEUE_TIMEOUT` у обычных диалогов; (в) при таймаутах или fallback’ах остаётся прозрачная наблюдаемость через логи `[dialog-engine][ai_fallback]`, `[ai_fallback]` и снимок лимитера.
   - **Результат:** задокументированный интеграционный сценарий `m9-ai-export-timeout` (и соседние кейсы) с шагами воспроизведения, временем и ссылками на логи; подтверждение, что текущая архитектура ролей/команд и AI-очереди согласована и не даёт скрытых конфликтов.
   - **Проверка:** целенаправленный прогон `двойной /export + пользовательский диалог` с захватом `wrangler tail`, `/admin/diag?q=ai-queue`, `ADMIN_EXPORT_LOG` и `systemCommands`. Скрипт подтверждает, что очередь сохраняет `active≤maxConcurrency`, `queued=0`, а пользовательские сообщения не получают `AI_QUEUE_TIMEOUT`; fallback логируется с `queueDetails` и `sources.*`. Все артефакты прикладываются к `memory-bank/logs/m9-ai-export-timeout-<date>.md` и сверяются с требованиями `ПРОВЕРКИ 12.1/13.1`.
   - **Результат задачи:**
   - Сценарии и артефакты опубликованы, инварианты подтверждены либо задокументированы отклонения.
   - При обнаружении аномалий созданные P1/P2 баг-задачи содержат ссылки на логи и шаги воспроизведения.
   - [x] 2025-11-26 `memory-bank/logs/m9-ai-export-timeout-2025-11-26.md` содержит полный сценарий и ссылки на все tail/diag логи, отклонений не выявлено.

#### Текущая диагностика (М9. Observability & Operations)
1. **[Шаг 1] Фиксация текущей диагностики** — обновить `memory-bank/diagnostics.md`, зафиксировать симптомы, модули и приоритеты. _Проверок перед следующим шагом нет._
   - [x] Статус: 2025-11-16 self-test контракт задокументирован, lossless `chat_id` подтверждён, `README.md` и `memory-bank/operations/admin-access.md` обновлены.
2. **[Шаг 8] Смягчение self-test OpenAI** — вернуть HTTP 200 с `openAiOk=false` и обновить адаптер для диагностического маркера. _Проверка 8.1: запрос `/admin/selftest?token=…` (метрики `openAiOk`, `errors`, статус-код)._
   - [x] 2025-11-11 `/admin/selftest` отдаёт 200 с `openAiOk:false`, лог `memory-bank/logs/selftest-soft-2025-11-11.md` фиксирует обновлённый маркер.
3. **[Шаг 9] Финальная синхронизация памяток** — выровнять `memory-bank` и RoadMap, зафиксировать статус проверок. _Проверок не требуется._
   - [x] 2025-11-26 RoadMap, `memory-bank/logs/m9-ai-export-timeout-2025-11-26.md` и `docs/operations/architecture-overview.md` содержат перекрёстные ссылки на все артефакты наблюдаемости.
4. **[Шаг 10] Продовый soft self-test 2025-11-11** — зафиксировать текущий ответ `/admin/selftest` (200, `openAiOk:false`, причина `missing_diagnostic_marker`), восстановить диагностический маркер Responses и устранить сопутствующие предупреждения (`safe fallback sent`, `failed to update admin export cooldown kv`). _Проверка 10.1: `/admin/selftest` → 200 с `openAiOk:true`, лог safe fallback содержит `reason` только при реальном сбое; `/admin/export` выполняется без предупреждений в логах._
   - [x] Продовый soft self-test задокументирован в `memory-bank/logs/selftest-soft-2025-11-11.md` и `memory-bank/diagnostics.md#diag-20251111`: ответ 200, `openAiOk:false`/`reason` заполнены без падения, предупреждения вынесены в отчёт.
5. **[Шаг 11] Восстановление `/admin status` для whitelisted админов** — router должен доверять `determineCommandRole`, даже если registry не знает ID, и регистрировать подтверждённые пары `команда ↔ пользователь`. _Проверка 11.1: боевой `/admin status` от администратора возвращает `admin-ok`, в tail нет `system_command_role_mismatch`, а повторная команда идёт без повторного запроса роли._
   - [x] **Шаг 11 (2025-11-25).** Router вызывает `determineCommandRole` при `role_mismatch`, после подтверждения регистрирует команду в `systemCommands`. Тесты `router.test.ts` покрывают сценарий «нет записи в registry → resolve через determine». Нужно подтвердить на продовом воркере, что `/admin status` больше не падает в отказ.
6. **[Шаг 12] Логирование AI fallback при нагрузке админов** — таймауты OpenAI должны приносить `queueDetails` (фаза, attempt, queueWaitMs, endpoint) в ошибке, а ядро — писать `[dialog-engine][ai_fallback]` перед отправкой сообщения «Не успел ответить…». _Проверка 12.1: стресс «двойной /export + пользователь» → tail содержит `[dialog-engine][ai_fallback]{…queueDetails…}` и запись `[ai][timeout]` с тем же `userIdHash`._
   - [x] **Статус 2025-11-26:** `memory-bank/logs/m9-ai-export-timeout-2025-11-26.md` агрегирует tail `wrangler-admin-export-rate-limit-2025-11-25.log`, `wrangler-tail-admin-2025-11-26.md` и `/admin/diag?q=ai-queue` (через `/admin status`) — таймауты сопровождаются `queueDetails`, пользовательские сообщения проходят без `AI_QUEUE_TIMEOUT`.
7. **[Шаг 13] Разделение ролей и команд** — зафиксировать, что системные команды не трогают AI-очередь и не создают Telegram-flood. _Проверка 13.1: сценарий «/export → повтор → сообщение пользователя» не порождает `[dialog-engine][ai_fallback]`, а админский кулдаун шлёт уведомление максимум один раз за TTL._
    - [x] **Шаг 13.1. Кулдаун `/export` без спама уведомлений.** Кулдаун хранит `expiresAt/noticeSentAt` в KV и подавляет повторные sendText до истечения 60 с. Тест `apps/worker-main/features/export/__tests__/telegram-export-command.test.ts` проверяет, что третья попытка в пределах окна возвращает 429 без нового сообщения, что предотвращает Telegram flood и потерю typing у админа.
    - [x] **Шаг 13.2. Tail `m9-ai-export-timeout`.** Tail-сессия и журнал `memory-bank/logs/m9-ai-export-timeout-2025-11-26.md` зафиксировали сценарий `/export` (двойной запрос) + пользовательский диалог: `router` останавливает `/export` на `rawRateLimit`, `determineCommandRole` разрешает whitelisted ID, очередь AI остаётся `active≤1/queued=0`.

**Задачи после создания предохранителей (М9)**
- 🆕 **Устранить задвоение приветствия при холодном старте.** Выявить причину повторной отправки «Привет, <имя>!»/«Привет!» при первом заходе через deeplink, задокументировать источник и исправление. Добавить ссылку на диагностику в `memory-bank/logs/` после воспроизведения. _Контекст: стартовый привет отправляется из `createStartCommandHandler`._
- 🆕 **Обновить ответ лимита до разъясняющего текста.** Заменить сообщение `🥶⌛️` по дневному лимиту на «🥶⌛️ Количество бесплатных запросов в день ограничено до N. Продолжение диалога доступно через N часов.» с расчётом числа запросов и часов на основе текущего окна лимитера.
- 🆕 **Единобразный fallback при таймаутах ответа.** Везде, где система сейчас отвечает вариантом «Не успел ответить вовремя — пожалуйста, отправь сообщение ещё раз.»/⚠️/🔁💬, заменить на фразу «Я на секунду отвлекся... Пожалуйста, отправьте сообщение ещё раз 🔁💬» и выровнять поведение маршрутов fallback.
- 🆕 **Инвентаризация системных сообщений.** Собрать документ со всеми захардкоженными системными ответами и событиями, где они отправляются, для сверки будущих изменений. [Список системных сообщений](docs/operations/system-messages.md).

**Риски и предохранители:** регулярные учения по инцидентам; уведомление on-call о любых изменениях; метрики покрывают все критичные сценарии.

**Критерии приёмки:** логи и метрики доступны, документация покрывает стандартные операции.

### Майлстоун 10. Post-MVP Enhancements (опциональный backlog)
**Цель:** подготовить пространство для расширения (например, .xlsx, мультиязычность, очереди).

**Идеи и проверки:**
- Экспорт `.xlsx` через очередь и R2.
- Автономное тестирование промптов Responses.
- Сегментация пользователей для рассылок.
- Вынести велкам-месседж из hardcode в переменную для быстрого редактирования, при необходимости исключить имя из приветствия ради приватности.
Каждый пункт потребует отдельной декомпозиции перед стартом.

### Финальные шаги. Внешние проверки созданного кода
**Цель:** подтвердить готовность решения к эксплуатации через независимые внешние проверки.

**Зависимости:** завершены майлстоуны 1–10.

**Шаги и проверки:**
1. Независимое код-ревью продуктовой командой.
   - **Результат:** внешний ревьюер подтверждает соответствие кода архитектурным принципам и критериям качества.
   - **Проверка:** подписанный отчёт ревьюера с перечнем замечаний и статусом их устранения хранится в `memory-bank/reviews/`.
   - **Внешние действия:** пригласить ревьюера, предоставить доступ к репозиторию и журналы прогресса.
2. Внешнее тестирование на предпроде.
   - **Результат:** QA-команда проводит сценарное тестирование бота на стенде, фиксируя результаты в общем отчёте.
   - **Проверка:** чек-лист тестов с отметками «пройдено/не пройдено» и ссылками на логи хранится в `memory-bank/tests/`.
   - **Внешние действия:** подготовить стенд, предоставить инструкции по тестированию и сценарии проверок.
3. Аудит безопасности и соответствия требованиям.
   - **Результат:** специалисты по безопасности подтверждают отсутствие критичных уязвимостей и корректную работу с секретами.
   - **Проверка:** акт аудита и список рекомендаций сохранён в `memory-bank/security/`, критичные пункты закрыты.
   - **Внешние действия:** организовать аудит, предоставить доступ к конфигурации секретов, ответить на вопросы аудиторов.

**Риски и предохранители:** задержка ревью может сдвинуть релиз; фиксировать в журнале все внешние взаимодействия и SLA; при выявлении критичных дефектов возвращаться к соответствующему майлстоуну для исправлений.

**Критерии приёмки:** все внешние проверки завершены, отчёты сохранены, замечания устранены или зафиксированы с планом работ.

## Журнал прогресса
Все записи о проверках, задачах-исправлениях и ретестах ведутся по требованиям раздела «Протокол проверки»: указываем статус (успех/неуспех/заблокировано), ссылки на артефакты и синхронизируем соответствующие задачи дорожной карты.
- 2025-12-05: Добавлена персистентная телеметрия рассылок (KV снапшоты по окружению/воркеру) с объединением in-memory данных в `/admin/diag?q=broadcast`; покрыто тестами восстановления после реинициализации обработчика, обновлена памятка `memory-bank/operations/broadcast-operations.md`.
- 2025-11-14: Устранена регрессия экспорта admin messaging errors — баррель `apps/worker-main/features/index.ts` теперь реэкспортирует `createAdminCommandErrorRecorder` и сопутствующие типы из `admin-access`, сборка `wrangler` не падает, `npm test` подтвердил, что diagnostics-роут продолжает собирать данные после обновления.
- 2025-11-21: Шаг М8.Ш4 закрыт — устранено двойное предупреждение «Текст не укладывается в лимит…», контрольный прогон `/broadcast` показал единичное уведомление и стабильный старт рассылки после `/send`. Памятки обновлены.
- 2025-11-20: Кодовая база восстановлена к коммиту `f0bfa1b02a8b84227ad5d2d21908573334e7763f` (PR #306) с задачами дебаунса текста, обязательного `/send` и `/cancel_broadcast`; боевой тест подтвердил работоспособность. Дополнительно создан `AGENTS.md` для обнаружения документации; новая ошибка с двойным предупреждением о лимите текста занесена в шаг М8.Ш4. См. журнал [`memory-bank/logs/roadmap-rollback-2025-11-20.md`](memory-bank/logs/roadmap-rollback-2025-11-20.md).
- 2025-11-10: Запущен и завершён шаг М7.Ш8 — подготовлены памятки по deeplink/UTM (`memory-bank/references/telegram-deeplink-utm.md`, `memory-bank/operations/export.md`), RoadMap дополнен статусом. Проверка: внутреннее ревью документов, автоматические скрипты не запускались (документационные изменения).
- 2025-11-09: Удалён бинарный артефакт `memory-bank/screenshots/2025-11-09-cloudflare-dashboard-secrets.png`, обновлён быстрый протокол (п.15) с запретом на добавление бинарников без специальной команды. Gate-чек `npm run check:roadmap` повторно запущен, зафиксирован вывод о несоответствии статусов М7/М8 (см. `logs/check-roadmap-2025-11-09.log`).
- 2025-11-07: Добавлены шаги М7.Ш5 и М7.Ш6 (issues [#458](memory-bank/issues/m7-parallel-work.md)/[#459](memory-bank/issues/m7-parallel-work.md)) после закрытия gate-чека; статусы перенесены из статуса ожидания в `✅`, протокол внешней проверки и синхронизации памяти опубликован (`memory-bank/tests/2025-11-07-admin-export-prod.md`, `logs/2025-11-07-admin-export-prod.csv`).
- 2025-11-07: Попытка выполнить требования self-test и задокументировать промпт Responses заблокирована отсутствием доступа к рабочему воркеру и интерфейсу OpenAI. Ожидаем действия команды с доступом к Cloudflare: требуется подтвердить модель/переменные опубликованного `pmpt_…` и прогнать `/admin/selftest` с реальным ответом (`openAiOk = true`). Подробности — [memory-bank/openai-responses-prompt.md](memory-bank/openai-responses-prompt.md) и лог [logs/admin-selftest-2025-11-07.log](logs/admin-selftest-2025-11-07.log).
- 2025-11-06: Закрыт gate-чек М7, созданы issues для параллельных потоков ([#458 — экспорт](memory-bank/issues/m7-parallel-work.md), [#459 — синхронизация памяти](memory-bank/issues/m7-parallel-work.md)). План: стартуем `work/export-external-check`, открываем черновой PR через **Compare & pull request**, после ревью выполняем `Rebase and merge`. Затем обновляем `work` и запускаем `work/memory-sync` с обязательным `git rebase origin/work` перед финальным PR; стратегия слияния — `Rebase and merge`, допускается `Squash` по согласованию.
- 2025-11-04: Попытка добавить секреты `OPENAI_MODEL`/`OPENAI_PROMPT_VARIABLES` через `npx wrangler secret put` заблокирована ошибкой `npm 403 Forbidden` при загрузке пакета `wrangler` (см. [logs/wrangler-secret-2025-11-04.log](logs/wrangler-secret-2025-11-04.log)). Без установки `wrangler` деплой и вызов `GET /admin/envz` недоступны; требуется доступ к registry или предварительно собранный бинарь.
- 2025-11-04: Продовая проверка `/admin/export` провалена — endpoint возвращает `404`, в `envz` отсутствует `admin_export_token`; заведен инцидент [memory-bank/issues/admin-export-regression-2025-11-04.md](memory-bank/issues/admin-export-regression-2025-11-04.md), шаг М7.Ш5 временно переведён в `🚧` (закрыт 2025-11-08 после внешней проверки).
- 2025-11-01: Выполнен gate-чек `npm run check:roadmap > logs/check-roadmap.log` — нарушений не выявлено, лог сохранён в [logs/check-roadmap.log](logs/check-roadmap.log).
  Ретест: повторный запуск `npm run check:roadmap` завершился без ошибок, вывод добавлен в [logs/check-roadmap.log](logs/check-roadmap.log).
- 2025-11-05: Выполнены модульные тесты экспорта (`npm test -- --run apps/worker-main/features/export/__tests__/admin-export-route.test.ts`, `npm test -- --run apps/worker-main/features/export/__tests__/csv-export.test.ts`), перезапущен self-test с сохранением лога ([logs/self-test-route.test.log](logs/self-test-route.test.log)); офлайн-проверка `/admin/export` задокументирована в [memory-bank/tests/2025-11-05-admin-export-check.md](memory-bank/tests/2025-11-05-admin-export-check.md) с контрольным CSV.
- 2025-11-05: Шаг М7.Ш4 переоткрыт — требуется прогон `/admin/selftest` против no-op AI; см. задачу [M7-SH4-selftest](memory-bank/tasks/m7-sh4-selftest.md).
- 2025-11-02: Дополнен быстрый протокол действий шагом по фиксации результатов проверок (коммит [fb273e1](commit/fb273e163f95d6df83dc8e0723205d0d70fb2f5e)).
- 2025-11-02: Протокол проверки активирован — `npm test` и `npm run validate-guards` выполнены успешно, `npm run lint` зафиксировал 12 ошибок (см. `memory-bank/ЗАДАЧА-ТОРМОЖЕНИЕ-проверки-не-закрыты.md`); продолжение работ заблокировано до устранения.
- 2025-11-03: Устранены ошибки `npm run lint`, закрыта памятка «ЗАДАЧА-ТОРМОЖЕНИЕ…», повторный запуск успешно завершён.
- 2025-11-03: Закрыт шаг М7.Ш4 — добавлены жёсткие проверки env (`OPENAI_API_KEY`, `OPENAI_MODEL`, `TELEGRAM_BOT_TOKEN`) и расширенный self-test с проверками OpenAI/Telegram, создан модульный тест `self-test-route.test.ts`.
- 2025-11-01: Прогнан `npm test` для подтверждения стабильности активной связки (М1, проверки активного стека); лог сохранён в `logs/test-2025-11-01.txt`, задача-торможение по проверкам остаётся открытой до полного комплекта запусков.
- 2025-10-28: Закрыт шаг М4.Ш1 — подтверждена миграция адаптера Responses, RoadMap обновлена статусом, сверены тесты `openai-responses.test.ts`.
- 2025-10-29: Закрыт шаг М8.Ш1 — реализованы очередь рассылок и админ-роут под флагом `BROADCAST_ENABLED`, добавлены тесты `admin-broadcast-route.test.ts` и обновлён HTTP-роутер.
- 2025-10-27: Адаптер OpenAI Responses мигрирован на `OPENAI_MODEL`/`OPENAI_PROMPT_ID`, обновлён маршрут `envz`, добавлены тесты `previous_response_id`.
- 2025-10-23: Инициализирована дорожная карта и создан каталог памяти (этап текущей сессии).
- 2025-10-23: Обновлена структура и правила дорожной карты.
- 2025-10-23: Сформирована детальная дорожная карта с майлстоунами 1–9 (до архитектурного обновления).
- 2025-10-24: Дорожная карта уточнена: устранены дубликаты, уточнены проверки и результаты шагов.
- 2025-10-24: Проведён критический анализ рисков, усилены проверки и предохранители по всем майлстоунам.
- 2025-10-24: Добавлены процедуры синхронизации ветки и проверки конфликтов перед PR.
- 2025-10-24: Реализован контур `DialogEngine`, определены контракты портов, подготовлены юнит-тесты (ожидают установки зависимостей npm).
- 2025-10-24: Подготовлен Telegram-парсер вебхука, команды `/admin*` обрабатываются до ядра, покрыто юнит-тестами `http/telegram-webhook`.
- 2025-10-24: Реализован адаптер Telegram messaging с ретраями и модульными тестами `apps/worker-main/adapters/telegram/__tests__/messaging.test.ts`.
- 2025-10-24: Настроен менеджер typing-индикации, роутер вызывает его перед ядром, добавлены тесты `apps/worker-main/http/__tests__/typing-indicator.test.ts`.
- 2025-10-24: Добавлен адаптер OpenAI Responses с таймаутами, ретраями и фильтрацией управляющих символов; написаны тесты `apps/worker-main/adapters/openai-responses/__tests__/openai-responses.test.ts`.
- 2025-10-24: Подготовлена стартовая миграция D1 `0001_init_dialog_tables.sql` с таблицами пользователей и сообщений.
- 2025-10-24: Реализован адаптер `createD1StorageAdapter` для Cloudflare D1 с модульными тестами `apps/worker-main/adapters/d1-storage/__tests__/d1-storage.test.ts`.
- 2025-10-24: Настроен KV-флаг `LIMITS_ENABLED` для гибкого отключения лимитов, добавлены обёртки и тесты `apps/worker-main/features/limits`.
- 2025-10-24: Добавлен KV-адаптер лимитов `createKvRateLimitAdapter` с TTL 24 часа и юнит-тестами `apps/worker-main/adapters/kv-rate-limit/__tests__/kv-rate-limit.test.ts`.
- 2025-10-24: Реализован нотификатор лимита `createRateLimitNotifier`, HTTP-роутер рассылает предупреждения и покрыт тестами `apps/worker-main/features/limits/__tests__/rate-limit-notifier.test.ts`, `apps/worker-main/http/__tests__/router.test.ts`.
- 2025-10-24: Добавлен админ-роут `/admin/export`, реализован потоковый CSV-генератор, обновлены тесты HTTP и экспорт-фичи.
- 2025-10-24: Добавлены финальные внешние проверки и обновлён быстрый протокол действий.
- 2025-10-26: Дорожная карта перестроена под Responses-only: добавлен майлстоун 1, перенумерованы шаги 2–10, зафиксированы проверки и задачи-торможения.
- 2025-10-26: Подготовлен план внешней проверки `/admin/export` для запуска после деплоя.
- 2025-10-27: Сформирован план ревизии модулей для Responses-only (М1.Ш3); обновлены `memory-bank/infrastructure.md`, `memory-bank/stable-builds.md`, незакрытые проверки отмечены блокером до выполнения адаптаций.
- 2025-10-27: Завершено обновление быстрых инструкций и памяток под Responses-only, закрыт шаг М1.Ш4 (проверка требований `OPENAI_MODEL`/`OPENAI_PROMPT_ID`, фиксация отказа от `assistantId`).

## Быстрый протокол действий
1. Проверить ветку и статус репозитория.
2. Ознакомиться с `RoadMap.md`, памятками в `memory-bank/` и актуальным списком проверок майлстоуна 1, подтвердить отсутствие новых зависимостей от `assistantId` и учёт требований к `OPENAI_MODEL`/`OPENAI_PROMPT_ID` (значения триммятся, `pmpt_` проверяется, переменные валидируются).
3. Если задача касается устранения ошибки или нестабильности, выполнить протокол [memory-bank/troubleshooting-protocol.md](memory-bank/troubleshooting-protocol.md) до начала изменений.
4. Составить микроплан и выполнить изменения ограниченной партией, указав в нём количество оставшихся проверок до завершения текущего шага.
5. **ПРОВЕРКИ ОБЯЗАТЕЛЬНЫ.** После каждой партии запускать проверки, просматривать изменения, обновлять журнал и памятки, фиксируя прогресс и оставшиеся проверки в отчёте.
6. **Результат проверок.**
   - Если все проверки пройдены: зафиксируй ссылки на логи в журнале прогресса, обнови RoadMap и памятки, удали соответствующие «задачи-торможения», затем переходи к следующей задаче.
   - Если хоть одна проверка упала: немедленно останови развитие по RoadMap, заведи/обнови «ЗАДАЧА-ТОРМОЖЕНИЕ: проверки не закрыты», опиши причину и план исправления в журнале, устрани проблему, перезапусти проверки и только после успешного результата переходи к следующему шагу.
7. Фиксировать в журнале прогресса рядом с временем шага его номер, номер текущего майлстоуна и ссылку на соответствующий список проверок.
8. Если требуется внешняя проверка созданного кода, то при команде «Следуй дорожной карте» создавай задачу с названием «ПРОВЕРКА», добавляй в Журнал прогресса строку «Переход к проверке» и указывай в отчёте фразу «ПРОВЕСТИ ПРОВЕРКУ» с инструкциями по проверке ниже.
9. При обнаружении невыполненной критичной проверки предыдущего шага немедленно добавляй в `memory-bank/` памятку «ЗАДАЧА-ТОРМОЖЕНИЕ: требуется проверка <описание>» (если уже есть «ЗАДАЧА-ТОРМОЖЕНИЕ: проверки не закрыты» — обновляй её содержимое) и останавливай продвижение по RoadMap до её закрытия.
10. Убедиться в чистоте истории: `git status`, `git diff --check`, разрешение конфликтов фиксируется в журнале.
11. После подтверждения внешними проверками завершения майлстоуна создать аннотированный стабильный тег `prod-YYYY-MM-DD[-label]`, занести ссылку и описание в `memory-bank/stable-builds.md` и запушить тег в `origin`.
12. Закоммитить изменения, подготовить PR через `make_pr`.
13. Отчёт о выполнении задачи всегда предоставляй на русском языке.
14. В отчёте указывай прогресс-бар с процентом пройденного пути, целью ближайшего майлстоуна и количеством задач до него, а также количеством шагов до ближайшей проверки.
15. Не добавляй бинарные файлы (скриншоты, архивы и т.п.) в репозиторий без специальной команды; артефакты подтверждения храни вне git и ссылайся на них из журналов. Если бинарный файл всё же попадёт в коммит, изменения не примут и работа над задачей окажется бесполезной — учитывай запрет при планировании.
