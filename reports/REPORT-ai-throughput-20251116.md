# X. 🚦 Пропускная способность AI-контура — анализ и план

- Дата: 16.11.2025
- Контекст: стресс-тест подтвердил устойчивость D1-ретраев; диалоги обрывались из-за таймаутов OpenAI. Требуется оценка предложенной схемы «таймаут + ретрай + бэкпрешер» и план усиления пропускной способности Telegram-бота на Cloudflare Workers.

## 1. Картина текущей реализации
- OpenAI вызывается напрямую из адаптера `adapters/openai-responses` без глобальных ограничителей: таймаут бюджета — до 20 с, допускается до трёх попыток подряд, логируются только общие события `[ai][request|ok|parsed]`. 【F:apps/worker-main/adapters/openai-responses/index.ts†L5-L7】【F:apps/worker-main/adapters/openai-responses/index.ts†L256-L404】
- Диалоговый движок (`DialogEngine`) запускает `sendTyping` заранее и удерживает его обновлениями до завершения AI-ответа, что позволяет безопасно ставить ожидание в очередь. 【F:apps/worker-main/core/DialogEngine.ts†L60-L129】【F:apps/worker-main/http/typing-indicator.ts†L46-L196】
- Команда `/admin/diag` уже поддерживает маршруты для биндингов и других проверок, поэтому расширение диагностик очереди не затронет ядро. 【F:apps/worker-main/index.ts†L310-L353】【F:apps/worker-main/http/router.ts†L305-L410】
- Конфигурация воркера собирается из Env и KV-флагов в `composition/compose.ts`, что позволяет внедрить новый слой адаптера без изменения core. 【F:apps/worker-main/composition/compose.ts†L1-L64】

### Выводы по рискам
1. **OpenAI — единственный неограниченный ресурс.** При всплесках мы упираемся в сетевые таймауты OpenAI, что подтверждает лог `[ai][non-2xx]` и отсутствие стороннего backoff.
2. **Cloudflare Worker разделяет CPU на все запросы.** Длительные AI-запросы занимают event-loop и блокируют новые диалоги, если не лимитировать одновременные fetch’и. Очередь из памяти воркера — самый быстрый путь.
3. **Observability недостаточна.** Нет метрик очереди, поэтому при деградации сложно отследить насыщение.

## 2. Цели и критерии
- **Управляемая параллельность**: гарантировать, что одновременно выполняется не более `AI_MAX_CONCURRENCY` fetch’ей; ожидающие запросы стоят в очереди до `AI_QUEUE_MAX_SIZE`.
- **Деградация без падения ядра**: переполненная очередь возвращает локализованный ответ и логирует событие `[ai][dropped]`.
- **Прозрачность**: расширить `/admin/diag?q=ai-queue` и логи `[ai][queue_*|timeout|retry]` с `requestId`, `userId`.

## 3. Варианты реализации и ожидаемая пропускная способность

| Вариант | Суть | Сложность | Оценка пропускной способности |
| --- | --- | --- | --- |
| **A. Обёртка-конкурент (`adapter` + in-memory queue)** | Создать обёртку над `AiPort.reply`: глобальный семафор + очередь в памяти воркера, конфиг из KV/Env (приоритет KV). Сигнализировать очереди через `Promise` + таймеры. | **Низкая**: изменения в адаптере + новая утилита очереди, без затрагивания core. | `AI_MAX_CONCURRENCY = 4`, `AI_TIMEOUT_MS = 12 000`, `avg_latency ≈ 6 с` ⇒ ~40 успешных ответов/минуту. Очередь 64 даёт до 70 одновременных диалогов при пиках (6 с ожидания ≈ 10 позиций → TTFB ~2,5–3 с). |
| **B. Распределённый контроль через Durable Object** | Вынести очередь в DO, поддерживать кросs-инстансовый баланс (каждый worker → DO). Позволяет удерживать общее состояние при масштабировании по регионам. | **Средняя**: потребуется новый DO, обработка отказов, миграции wrangler.toml. | При `AI_MAX_CONCURRENCY = 8` на DO и `avg_latency ≈ 6 с` ⇒ ~80 ответов/мин. Очередь 200 (в DO) даёт ≈220 активных диалогов, но добавляет сетевые RTT (TTFB ~3–3.5 с). |
| **C. Шардинг по userId + адаптивный семафор** | Поддерживать локальный семафор, но динамически адаптировать `AI_MAX_CONCURRENCY` по нагрузке (метрика success/timeout). Добавить backoff на уровне webhook (откладывать обработку до N мс). | **Высокая**: сложный контроль состояния, требуется хранение статистики и обратное влияние на обработку сообщений. | Потенциал до 120 ответов/мин при `AI_MAX_CONCURRENCY` 4→12 в пиках, но нужно гарантировать стабильность D1 и Telegram rate limit. |

**Рекомендация:** начать с варианта A (минимальный time-to-fix), затем по необходимости эволюционировать к B для мульти-инстансовой стабильности. Вариант C пригоден после подтверждения производительности и появления данных о реальных пиках.

## 4. Предлагаемая реализация (Вариант A)

1. **Обёртка AI-порта.**
   - Добавить модуль `adapters/openai-responses/concurrency-limiter.ts` с семафором и очередью (конфиг `maxConcurrency`, `maxQueueSize`, `timeoutMs`, `retryMax`).
   - Конфигурация: `readConfig` → KV → Env → дефолты. Env-ключи: `AI_MAX_CONCURRENCY`, `AI_QUEUE_MAX_SIZE`, `AI_TIMEOUT_MS`, `AI_RETRY_MAX`.
   - Логирование: `console.info('[ai][queue_enter|queue_leave]', {...})`, `console.warn('[ai][retry]', {...})`, `console.error('[ai][timeout|dropped]', {...})` с `requestId`, `userIdHash` (первые 8 символов SHA-256).

2. **Переработка ретраев.**
   - Ограничить `maxRetries` до `AI_RETRY_MAX` (дефолт 2) с экспоненциальным backoff + jitter (1.0, 2.0, ×(1±rand*0.2)).
   - Таймаут запроса — `AI_TIMEOUT_MS` (дефолт 12 000 мс), AbortController.
   - Логирование `[ai][retry]` с номером попытки и причиной (HTTP-код, network, timeout).

3. **Бэкпрешер и мягкий отказ.**
   - При переполнении очереди сразу возвращать `AiPort.reply` с текстом из новой утилиты `getFriendlyOverloadMessage(locale)`; дополнительно лог `[ai][dropped]`.
   - Поддержка typing: очередь удерживает promise; `DialogEngine` уже поддерживает бесконечный typing, поэтому дополнительных вызовов не требуется.

4. **Диагностика.**
   - В `features/admin-diagnostics` добавить обработчик `/admin/diag?q=ai-queue` с данными `{ active, queued, maxConcurrency, maxQueue, droppedSinceBoot }`.
   - Хранить счётчики в модуле семафора (использовать `AtomicInteger` через замыкание).

5. **Тестирование.**
   - Юнит: очередь режет конкуренцию, retry не трогает 4xx ≠429, очередь переполняется → логирует drop.
   - Интеграция: мок `AiPort` для сценариев 8 параллельных сообщений.
   - Смоук: через `/admin/d1-stress` + скрипт отправки сообщений (подготовить инструкцию).

## 5. Дополнительные улучшения
- **Метрики в logs/**: прокинуть JSON-логи в workers analytics (подготовить `scripts/analyze-ai-queue.mjs`).
- **Fallback OpenAI Regions** (опционально после варианта A): возможность смены `baseUrl` при массовых 5xx.

## 6. План задач
1. **RFC & каркас очереди.** Подготовить черновик реализации и структуру конфигурации.
2. **Код семафора и обёртка AiPort.** Реализовать очередь, интегрировать логирование и мягкий отказ.
3. **Диагностический маршрут `/admin/diag?q=ai-queue`.**
4. **Тесты (юнит + интеграция).** Написать и автоматизировать сценарии из требований A/B.
5. **Smoke-прогон и отчёт в RoadMap.** Собрать артефакты (лог, диаг-скрин), закрыть шаг X.

После завершения шагов 1–5 оценить показатели очереди на проде и решить, нужен ли переход к варианту B.
